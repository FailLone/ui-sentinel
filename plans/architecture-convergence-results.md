# 架构收敛验证记录

状态：执行中。对应 [计划](./architecture-convergence-plan.md)。此文记录新的持续探索目标；前轮阴性研究已经结束，不覆盖其历史成绩。

## A：评分协议与离线复评

新增 `evaluation/private/recovery-protocol.ts`，协议 `recovery-v2`。历史 minimum evaluator 不变。C2 允许保存拦截证据后通过正常输入恢复成唯一 paid 订单；必须有对应 UI 和业务结果。恢复路径是否使用正常操作仍须核查工具轨迹，自动业务评分不是安全动作证明。

C5 新增测量类型、同一订单 UI、公开 canRetry=true 响应关联要求；保留旧 evaluator 的连续时间窗口、目标绑定、假设和截图要求。所有场景必须显式接受 Agent 结束。反例测试覆盖缺失发现、重复订单、错误 UI/业务结果、无主动结束、错误测量类型、缺失 eligibility、短测量窗口。

旧批次离线复评：`data/oss-compare/2026-09-23T16-58-30-720Z/recovery-v2-rescore.json`。只有 Stagehand C2 从原协议失败变为新协议通过；原始三例在新口径下 current 2/3、Stagehand 3/3、Browser Use 1/3。此前已核查 Stagehand Tab/Enter 与实际键盘输入、唯一 paid 订单及截图证据。此处是协议诊断，不是新候选独立验收，也不把旧 `score` 改写为通过。

## B：准备同期完整任务对照

六组：current-low/current-off、stagehand-low/stagehand-off、browser-use-off/browser-use-flash。每组 C0/C2/C5 各一轮，共十八轮筛选（不是 minimum 正式十八轮）。美元硬上限 $3，单任务/单请求预算不增加；每轮网关独立冻结推理模式，后续轮次不能继承上轮覆盖值。新增测试验证该隔离。

Browser Use flash_mode 已用真实原生循环、本地浏览器、确定性模型验证质量工具链：`data/native-fixture-check/2026-09-24T05-04-15-409Z/`。实际发出的输出 schema 仅 memory/action，没有 evaluation/next_goal 字段。该测试不调用真实模型，也不证明检查质量；官方参数依据见计划中的链接。

执行命令：`pnpm exec tsx scripts/experiments/oss-compare.ts --study convergence`。必须在干净提交、构建和检查通过后执行，运行前 manifest 冻结六组配置和新旧 evaluator 哈希。旧命令模式仍使用历史评分和 low 配置，避免改变旧批次复现含义。

### 首批中止：历史隔离与原生计时缺陷

冻结 `851b226`，批次 `data/oss-compare/2026-09-24T05-06-55-086Z/`。已保存七轮完整记录，在第八轮中止；不将此批用于架构排名或推理配置的因果比较，不补齐其剩余格子后拼接成绩。

实际请求证明共享 `environmentId=arena` 导致后运行的当前执行器加载前轮生成的 journey：C0-current-low 首次输入有 0 个 availableJourneys，C0-current-off 首次输入有 1 个。这违反相同历史起点；早先报告的 45.192s 与 27.806s 不能归因于推理开关。发现后停止父调度、取消原生 worker 并关闭本批服务；没有孤立的试验进程继续运行。所有旧记录和取消输出保留。

此批共开始 92 个上游请求，91 个已落账，已知费用 $0.056840680；中止时另有 1 请求未结清，保守占额 $0.008348340，不按零成本算。详见 `invalid-batch.json` 和 `interrupted-accounting.json`。

另一个明确的接线缺陷：Browser Use flash 在 C0 中主动重新导航，导致页面内计时状态重建为 id=0/dispatchAt=0；旧适配器把首次渲染减去零，生成约 1.79e12ms 的虚假响应时长及四条规则误报。真实点击响应是 97–134ms。这是适配器错误，不是 Agent 判定能力退化。

唯一修正批次采用 `architecture-convergence-screen-2`：每轮使用独立数据库和应用服务进程，正式 API 仍使用受支持的 arena 环境；付费前检查 runs/journeys 均为空，冷启动模型输入必须不含继承的导航片段，网关在请求转发前强制验证；若违反立即停止整批。原生计时仅接受本文件内真实输入，按 documentId/actionId 去重，刷新不生成从 Unix 零点开始的伪测量。未测到的跨文档导航时延不冒充有效零时延。

测试覆盖真实历史存储的环境隔离、付费前请求拒绝、页面刷新不产生伪响应及刷新后新动作不被错误去重。修正不改变 Agent 提示词、推理预算或验收答案。两项问题在同一冻结修正批次中处理；不因模型后续质量失败再追加这一批的修正额度。

修正批次启动前验证：49 个测试文件、337 个测试全部通过；类型检查、完整构建、157 个文件格式检查和 `git diff --check` 通过。测试覆盖本次隔离与导航后计时问题；不替代真实模型筛选。


### 修正后完整筛选：已完成，未替换默认架构

冻结 `b0837f6`；批次 `data/oss-compare/2026-09-24T05-29-48-781Z/`。18 轮全部结束，使用 18 个独立数据库和应用服务进程，每轮起始 runs/journeys 均为空；网关无历史污染拒绝。445 个报告引用产物均通过正式 API 存在性/格式检查，未出现多订单。`decision-index.json` 索引每次工具选择/用量；`semantic-review.json` 逐轮记录证据核查和源文件哈希，原自动分数不改写。

| 配置 | 自动通过 C0/C2/C5 | 实际模型请求合计 | 三例报告时间合计 | 已知费用 USD |
| --- | --- | --- | --- | --- |
| current-low | 3/3 | 23 | 131.491s | 0.026838168 |
| current-off | 1/3 | 29 | 113.189s | 0.025484608 |
| stagehand-low | 2/3 | 72 | 289.676s | 0.045846944 |
| stagehand-off | 2/3 | 75 | 185.808s | 0.039032424 |
| browser-use-off | 2/3 | 29 | 211.763s | 0.023767224 |
| browser-use-flash | 0/3 | 20 | 129.278s | 0.003930840 + 1 次未知费用 |

合计 248 个上游请求，已知费用 $0.164900208；1 次中断请求费用未知，保守占额 $0.004916380；没有未释放在途预留。全部关闭推理的已知 usage 均未出现非零 reasoning tokens。表内失败也计时/计费，不能把失败组的较低总时间解释为效率提升。每格只有一轮，不能据此估计稳定成功率、P95 或因果性能增益。

证据揭示了不同失败机制：

- **简单导航已很便宜。** 当前执行器 C0 low/off 都是 5 次调用，10.661s / 8.640s。Stagehand 同场景两组均 21 次调用；其原生 ariaTree、act 内部定位和额外确认造成多层调用。Browser Use 普通组 6 次调用、38.069s，通过，但模型外启动/操作等待也占时间。
- **异常后的决策与收尾仍是主要瓶颈。** current-low C2 通过，但 76.566s 中模型占 71.818s，发现遮挡之后仍反复查询/查看。current-off C2 保存了拦截发现，却重复读取规则、尝试被挡住的点击，最终 no-progress 退出，没有主动结束。
- **关闭推理不等于减少整个任务工作量。** current-off C5 连续测量了五个约 6 秒窗口、登记两个假设并提交重复发现，14 次请求 / 68.443s，仍没有主动结束；low 只需一个 5.083 秒窗口，10 次请求 / 44.264s，通过。
- **开源原生循环可用，但不能直接整体替换。** Stagehand 两组 C5 都完成了 5 秒 actionability 测量和结束；C2 却均耗尽 30 次调用。low 组出现 snapshotId/qualityRef 混用及重复调查已保存问题，off 组重复 act/ariaTree 后仍未结束。Browser Use 普通组 C2 正常 blocked 结束，但 C5 把五秒可见性测量用于不可操作结论，被证据守卫拒绝。flash 在 C0 重复回首页后请求超时、C2 猜测不存在的 /cart URL、C5 未完成未知缺陷验证即结束。
- **报告也需约束措辞。** stagehand-off C5 的核心“五秒不可操作”结论有证据，但使用 permanently disabled 超出有限窗口可证明的范围；保留该语义问题，不把自动通过等同于报告每句话都已证实。

本阶段决定：保留 current-low 为后续合格基线，不全局关闭推理，不采用这几组开源配置整体替换。这个结论限于这些冻结配置、模型与任务；不代表开源框架在其他模型/配置下无价值。也不能证明当前架构最优：异常决策有明显重复，且本批尚无独立重复或陌生场景验证。

C 阶段应优先检验决策契约：让当前事实、已经完成的调查、仍欠缺的验证和可执行工具保持一致。现有 finalizing 输入仍暴露 page_act、hypotheses_record 等运行时不允许的工具；已完成测量也未形成可靠的下一步决策边界。候选必须保留自主提出新问题与追加有效调查的能力，不能只强制调用 finish 来提高分数。前轮摘要实验仅在原输入上追加重复摘要，未取得质量收益；本轮不重复该方案。先用冻结状态验证契约变化，再以完整路径验证；不再重复六组矩阵直到出现赢家。


## C1：能力与调查生命周期契约重放，未采用

冻结 `a59d926`，批次 `data/decision-replay/2026-09-24T05-56-24-073Z/`。6 个真实冻结状态 × 两组 × 两次，24 请求全部结束；没有浏览器或工具执行。候选生成调查生命周期索引、排除缺少合法对象及阶段禁止的工具、枚举已有假设/当前元素引用；原系统约束和原始事实保留。相关 7 项测试与类型检查通过。

逐项语义审核见 `semantic-review.json`：基线有效下一步 9/12，其中两次是上游连接中断/请求超时，一次重复观察；候选有效主要动作 11/12，仍有一次在已知拦截且无关闭控件的状态点击非交互活动标题。一次候选只记录未验证范围，计为合理报告准备，不计为主动结束；一次候选提交的五秒结论有效，但额外“约 19 秒”描述依赖未声明的订单 ID 时间格式，不应保留。有效主要动作并不等于输出所有措辞或完整路径通过。

包括失败在内基线 171.578s、候选 166.865s，不能将包含两次网络失败的 2.7% 差异解释为提速。基线已知费用 $0.011198872 + 2 次未知，候选完整费用 $0.016518952；整批已知 $0.027717824，未知保守占额 $0.012168100。未满足 12/12 质量或性能/完整费用门槛，不接入产品默认路径，不补跑失败格子。

结论：让阶段、引用和已有证据更明确有局部价值，但仅调整上下文/工具接口不足以可靠消除异常后的重复操作。当前保留的实验实现不是已采用架构。后续需要改变决策与验证的职责边界，或在独立全路径复验中证实基线已足够稳健；不能从这个阴性结果推出当前架构最优。


## C2：单次有类型调查，准备真实路径诊断

新增默认关闭的 `investigation_check`，由 Agent 声明条件/目标/窗口，执行层完成登记、采样、有限窗口判断和保存。复用保留原测量时间；新节点、新操作、页面版本变更或显式新窗口要求不能误用历史结果。假设的支持/反驳只针对生成的有类型命题，原 Agent 问题作为来源记录，不能借原问题措辞把可操作性结果升级为像素遮盖或永久故障。

51 个测试文件、346 个测试通过；构建通过。真实浏览器测试覆盖复用、节点替换、健康可操作反例、可见性与可操作性区别、取消。`data/atomic-fixture-check/2026-09-24T06-17-42-677Z/` 已通过真实 Server/Mastra/Chromium、确定性本地模型、正式报告 API 的工具链预检：三次决策完成两次同题请求（实际只测量一次）、一个假设、一个发现、一次主动结束，零付费请求。预检初次因 ARENA_PORT 未与测试端口一致被 API 拒绝，修正测试配置后通过，未放宽产品 API。最终冻结前还要核查有限命题措辞与完整构建。

最终工具链预检 `data/atomic-fixture-check/2026-09-24T06-19-25-625Z/` 九项断言通过，包含有限命题边界；完整构建与 162 文件格式检查通过。候选保持默认关闭，进入已声明的六轮诊断。


### C2 六轮真实诊断：完整通过，进入独立复验

冻结 `ee7242e`；批次 `data/oss-compare/2026-09-24T06-20-08-192Z/`。baseline/current-atomic 在 C0/C2/C5 均通过；187 个产物经正式 API 验证可读取，全部主动结束，没有重复订单或重复发现。候选 C5 实际调用了新工具：一次 actionability 测量（5.074s、26 个 false 样本）、一个有界发现、一个已解决假设，随后明确结束；未知问题由模型识别并绑定，规则库未新增规则。

| 场景 | baseline 时间 / 调用 | atomic 时间 / 调用 |
| --- | --- | --- |
| C0 | 11.014s / 6 | 12.883s / 6 |
| C2 | 53.539s / 9 | 100.099s / 9 |
| C5 | 73.694s / 12 | 28.713s / 7 |
| 合计 | 138.247s / 27 | 141.695s / 22 |

完整费用 baseline $0.032110488、atomic $0.028154952，无未知费用。合计调用下降 18.5%、费用下降 12.3%，但耗时增加 2.5%，尚不满足整体提速门槛。C2 未调用新工具，两组模型输出/服务速度均有差异；不能把单对的时间差归因于新工具效果。C5 的调用减少有执行轨迹支持，足以进入独立复验，不足以启用默认配置。有限测量窗口不证明事件发生后已经流逝的首五秒，这一边界保持显式。

## D1：独立复验进行中，发现执行干预造成的误报

冻结 `212e772`，批次 `data/oss-compare/2026-09-24T06-29-30-605Z/`，完整结论待 36 轮终结和审核。前 24 轮 C0–C3 均通过。C2 候选未调用新调查工具，表现差异涉及工具接口/说明变化与模型选择，不能将其速度差异全部归因于机械步骤合并。

`C4-current-low-1` 自动评分失败：273.389s、16 请求，最终明确结束且业务结果正确，但生成了一个不应成立的支持性发现。轨迹显示先收到 rejected/canRetry=true 与可操作的 Try Again，随后 Agent 仍点击重试；执行器在网络层阻断第二次 POST，产生 `write:denied`，真实后台仍只有一笔订单。页面的请求异常处理将恢复按钮留在 disabled/Retrying 状态。Agent 在登记假设时已经提及 write-denied，却继续做六秒测量并保存为站点缺陷。有效测量证明了干预后的页面状态，不能证明这是未受干预业务流程的故障。

对应 run `run-5f5a5f58-0b44-45e7-a0bf-cb2d86851fc3`、finding `finding-2026c499-b2a4-4957-9861-111c064050a5`；原自动评分保留为 fail，不重跑或剔除。接续的候选 C4 前两轮通过，但尚不能声称机制上已消除这一风险。后续架构决定必须处理执行器干预与证据来源的边界：安全策略避免了重复订单，却没有阻止干预后的证据被当作原网站缺陷；只靠提示词和较少调用不足以提供这个保证。当前批次继续按冻结协议运行，不中途修改执行器。

### D1 完整结果：候选通过独立质量比较，尚不默认采用

36 轮已全部终结，无在途实验进程。36 个独立空数据库，1,030 个报告产物全部经 API 读取验证，无重复订单。唯一重复写入尝试为上述基线 C4，其后端请求被阻止；该轮保留 fail。原始记录、决策索引与语义复核分别在 `records.json`、`decision-index.json`、`semantic-review.json`，含源数据哈希。

| 指标 | current-low | current-atomic |
| --- | --- | --- |
| 完整质量通过 | 17/18 | 18/18 |
| 实际上游请求 | 141 | 108 |
| 报告时间合计（含失败） | 847.762s | 481.862s |
| 模型请求时间合计 | 783.179s | 417.202s |
| 已知费用 USD | 0.139086600 + 1 次未知 | 0.089722008 |

批次总已知费用 $0.228808608；一次中断请求未知费用保守占额 $0.006074320，无未释放在途预留。调用减少 23.4%；约 43.2% 的总时间差包含基线误报长尾与网络失败，不能宣称同等质量下满足 15% 性能采用门槛，也不剔除失败只算成功子集。候选 18/18 属于独立质量收益证据，不代表模型不会再选错动作。

| 场景（三次合计） | baseline 秒 / 调用 | atomic 秒 / 调用 |
| --- | --- | --- |
| C0 正常成功 | 74.449 / 17 | 62.703 / 15 |
| C1 可关闭遮挡 | 58.632 / 18 | 76.759 / 18 |
| C2 不可关闭拦截 | 238.172 / 30 | 117.497 / 20 |
| C3 改名/移位 | 26.745 / 15 | 33.397 / 17 |
| C4 预期拒绝 | 317.487 / 29（一次失败） | 93.164 / 18 |
| C5 未知恢复缺陷 | 132.277 / 32 | 98.342 / 20 |

C5 候选三次均恰好调用一次 investigation_check，分别产生 5 秒、26 个 false 样本、一个已解决假设和一个有界发现，然后主动结束；没有已有学习规则。三对调用从 10/10/12 降至 7/6/7，报告时间分别 42.108/42.068/48.101s 对 31.169/20.346/46.827s。这支持机械步骤合并的机制，仍仅限三次重复和这一缺陷类型。C1/C3 没有提速；C2 未使用新工具，不能把它的全部收益归因于调查执行本身。

下一步先按冻结执行器完成陌生业务比较，再补执行干预的证据边界与相关验收。D2 使用同一个 Server 构建哈希，不因这次 C4 失败中途改执行器或提示词。候选默认仍关闭；最终采用还需要陌生业务质量、干预反例、产品整合与正式验收。

### D2 预检

新增独立预约页面及私有 oracle，22 项定向测试通过，包含三个真实浏览器流程与 17 种错误证据反例；完整套件 52 文件、368 测试通过。测试中只读报告对象的修改方式经类型检查修正后，定向测试再次通过，类型检查通过。未调整业务执行器或模型提示词。付费前冻结 H0/H1/H2 × 两组 × 三次共 18 轮、$2 上限，保持 D1 模型、预算、规则与冷启动条件。

### D2 完整结果：迁移能力通过，模型长尾仍未消失

冻结 `2129324`，批次 `data/oss-compare/2026-09-24T06-59-24-059Z/`，18 轮完成。使用与 D1 相同的 Server 构建，18 个数据库起始 runs/journeys 均为空，470 个报告产物全部可读取，未出现重复写入或干预事件。假设/发现、实际恢复控件、测量条件/窗口和原数据哈希保存在 `semantic-review.json`。

| 指标 | current-low | current-atomic |
| --- | --- | --- |
| 完整质量通过 | 7/9 | 9/9 |
| 实际请求 | 68 | 61 |
| 报告时间合计（含失败） | 796.807s | 403.815s |
| 模型请求时间合计 | 770.612s | 356.538s |
| 已知费用 USD | 0.064993880 + 4 次未知 | 0.065153616 |

整批已知 $0.130147496，未知保守占额 $0.024632440，无在途预留。基线 H2 第 2/3 次分别在两次连续 60 秒请求超时后 execution-error，未完成未知调查和主动结束。它们是实际请求失败，不能补跑或当作通过；四次未知费用不按零计。候选没有出现这些失败，但不能据此断言候选消除了供应商长尾，或把约 49.3% 总时间差称为同等质量的性能收益。费用比较不完整，也没有证据支持本批精确降费比例。

| 场景（三次合计） | baseline 秒 / 请求 | atomic 秒 / 请求 |
| --- | --- | --- |
| H0 新布局成功路径与勾选前置条件 | 110.065 / 16 | 79.022 / 16 |
| H1 失败但正常恢复 | 243.419 / 27 | 128.626 / 21 |
| H2 恢复按钮持续不可操作 | 443.323 / 25（两次失败） | 196.167 / 24 |

候选三次 H1 都用有界调查反驳了禁用假设，没有生成支持性缺陷；独立 oracle 确认实际 Resume reservation 控件可见、启用且命中正常。三次 H2 均一次调查产生恰好一个发现，测量为 actionability、约 5.08 秒、26 个 false 样本，并与同一预约的公开 failed/canRetry=true 和截图关联。按钮文案没有 retry，正常前置条件也没有被误报。候选 H2 仍有一次 98.191s/9 请求，不能声称固定调用数或最坏耗时已经解决。

本阶段支持将单次有类型调查作为产品候选：D1 18/18、D2 9/9，旧循环分别 17/18、7/9。采用依据是这两个冻结批次的质量表现及减少机械决策的轨迹，不是声称达到同等质量 15% 提速/完整降费门槛，更不证明绝对最优。仍须完成 D3 的正确性保护与正式产品验收。

## D3：执行干预的证据保护

源代码修复在 D2 使用冻结构建时另行准备，期间未构建或运行浏览器测试；终结后才开始验证。执行器同步记录 write/access/popup 干预，快照、测量与派生截图保留来源；自动规则与时间谓词返回 unknown，持久化阻止受影响证据被提升为 supported/refuted。假设来源另存不可变事件，避免先改状态/换证据再绕过限制。规则候选生成也必须检查来源，人工确认不使受干预测量变成未受干预业务的事实。

干预前的发现与业务结果保留；后续整个 Run 的新页面状态保守视为未验证。Agent 清空分支、滚动或导航不能消除服务端缺口；短收尾规范化为 unverified-scope，报告独立恢复干预与受影响产物。对历史无来源标记的产物维持兼容，不追溯宣称有相同保证。

第一轮定向测试 3 文件、49 项通过，覆盖真实网络拦截导致恢复按钮禁用、自动规则/声明式规则/两种未知调查不误报、旧发现保留、混合/派生证据、报告与业务结果。随后补充假设来源不可被改写、学习候选不得使用受影响证据的回归，正在做完整测试和构建。默认候选开关仍关闭；付费前冻结正式验收配置。

完整验证 53 文件、373 测试通过，类型检查与完整构建通过；168 文件格式检查通过。`data/atomic-fixture-check/2026-09-24T07-30-23-229Z/` 的实际编译后 Server/Mastra/Chromium、正式报告 API、本地确定性模型预检九项断言全部通过，零付费调用。之后只将结果文字的“evaluated interval”改为“declared interval”，避免无有效样本时暗示已经覆盖整个窗口；四项调查测试与完整构建再次通过。候选保持显式开关，准备冻结真实 smoke 与正式 minimum。

### D3 正式验收失败：报告与持久化状态不一致

冻结 `971659a`，`data/acceptance/2026-09-24T07-33-56-551Z/` 的真实 DeepSeek/Qwen smoke 通过。minimum 实际只创建两个 Run：C0-1 通过，C0-2 未通过；后续 16 条是队列非空导致未创建的 invalid 记录，不能称为完成了 18 次模型执行。总费用 $0.008428880，无未知费用。

C0-2 的正式报告声明 completed/success、4 个动作、5 次模型调用、8.936s，但报告只有 8 个事件和 4 个产物，缺少结束与业务证据；最后事件是首个 page_act 开始。进程终结后独立读取 SQLite，实际 Run 仍是 running，事件同样停在 seq 7，而真实业务后台已有一个 paid 订单。数据库 integrity_check 为 ok，存在 journal 文件。不能将其解释为模型未调用 finish，也不能把报告中的 completed 单独作为成功证据。根因尚未确定，暂不改模型或提高预算。

零费用诊断：基础文件数据库并发读写及重新打开测试通过；四轮原子调查预检通过；按原批次工具调用顺序、包含跨轮 journey 的六轮真实 Server/Mastra/Chromium 重放通过（`data/persistent-replay/2026-09-24T07-46-30-168Z/`）；加入正式私有 resetAndVerify 流程的另外六轮也通过（`data/persistent-replay/2026-09-24T07-48-37-626Z/`）。这些不能证明原问题已修复，只排除了必然发生的串行收尾或导航复用错误。

下一项诊断限定为三个真实 C0，固定原模型和预算、整批费用上限 $0.10，附加 SQL 查询/连接顺序日志；遇到证据不一致立即停止，不强制重置、不中途换配置、不计入正式 minimum。诊断代码和原始数据留在 `data/`，产品执行器未改。候选仍默认关闭，正式验收与规则复查仍未完成。

该诊断 `data/persistence-live/2026-09-24T07-50-36-345Z/` 三轮全部通过，费用 $0.011096344；4,912 条 SQL/连接日志未出现错误或释放仍在事务内的连接。终止进程后独立 SQLite 读取的三轮状态、事件数（92/85/99）与正式 API 一致。SQL 日志会改变时序，因此阴性结果不证明原缺陷消失，也不归因于 libSQL/Mastra 的已知 bug。公开上游检索未找到能够确认同一机制的依据。

后续保留同一生产构建、模型、预算，给完整 minimum 增加外部 SQL 观测，再收集一次固定 18 轮；新增观测的源码/哈希随批次保存。原失败独立保留，不合并两批成绩，不把观测本身称为修复。若再次发生，可据 SQL 时间线定位具体写入/读取；若不再发生，持久化风险仍须在最终结论中披露并补稳定的回归边界。

### 附加观测的完整批次：16/18，模型长尾仍阻止发布

`data/acceptance/2026-09-24T07-53-11-462Z/`，冻结文档提交 `9cc3469`，生产构建与 `971659a` 相同。真实 smoke 通过，18 个 Run 均实际执行；16 通过，C2-2/3 分别在 148.207s/157.741s 以 model-request-timeout 结束。两者已保存支持性的指针遮挡发现、后台零订单，但连续两次 60 秒请求均未执行工具、没有主动结束。不能以发现正确或调用数较少把这两轮计为通过。

96 次业务 Agent 请求、报告时间合计 731.792s（包含失败）；已知整批费用 $0.082448584，另有四次未知费用，保守占额 $0.025200560。505 份产物通过 API 检查。停服后用独立 SQLite 读取所有 Run、事件 ID/顺序/类型和产物 ID，与 18 份正式报告全部一致，数据库 integrity_check 为 ok；记录见 `durability-audit.json`。SQL 观测源码、哈希和完整日志均已归档。原持久化异常没有复现，但尚未解释，不能称为修复。

所有 C2 都复用了历史导航段，并在到达 Checkout 后因质量异常 handoff；其中 C2-1 能主动结束，另外两轮先重复查看活动文案/页面再超时。冷启动 D1 没有跨轮导航记忆，因此不能把两批差别全部归因于原子调查或模型速度；导航复用与失败的因果关系仍未建立。输入已有 supported finding、blocked finishAdvice、空 applicableGaps，简单再加一句“请结束”不是有证据的修复。

另外发现观测缺口：旧网关只在流完整结束后保存上游事件，中断时丢掉部分响应及 responseId；SDK 的 delta 时间也不能保证覆盖供应商专有 reasoning 字段。因此现有记录不能区分这四次超时到底是持续推理还是无有效输出。已补网关部分事件持久化、传输完整标记、收到的字节数，以及 reasoning/content/tool 三种首增量时间；保持凭据脱敏、未知费用占额和原模型预算。九项网关测试通过，包含碎片 UTF-8、截断流、未终止 JSON 和最终费用只记录一次的反例。

新增可重复的零付费预检 `pnpm exec tsx scripts/experiments/persistence-preflight.ts`（先 build）。它运行长期存活的实际 Server、六个 C0、跨轮 journey、正式评分/产物 API，关闭服务后再用 Node 自带 SQLite 引擎独立比较持久记录。`data/persistence-preflight/2026-09-24T08-06-56-689Z/` 六轮全部通过。它覆盖新的集成边界，不能替代真实模型验收或证明偶发问题根因消失。

当前结论调整：原子调查仍有独立质量/调用收益，但当前完整组合未通过正式验收，不能默认采用。下一阶段针对超时后的决策恢复验证新假设；先补观测，再做冻结状态诊断，不重复盲跑十八轮，也暂不启动依赖合格执行器的学习复查。

## 剩余瓶颈复核：减少决策往返，而非每次输入都更小

D1/D2 原始请求另生成 `bottleneck-audit.json`，包含全部计划轮次（包括失败），逐请求输入大小、历史窗口、页面摘要和元素数。这里的模型时间是网关请求遥测与报告时间之比，不是独立 CPU profiler 结论，也不从少量重复推算 P95。

| 指标 | D1 baseline → atomic | D2 baseline → atomic |
| --- | --- | --- |
| 模型请求时间 / 报告时间合计 | 92.4% → 86.6% | 96.7% → 88.3% |
| 请求时长中位数 | 2.493s → 2.003s | 5.919s → 4.012s |
| 最长请求 | 60.006s → 23.900s | 60.000s → 26.359s |
| 每轮首请求体字节数中位数 | 22,291 → 25,359 | 21,742 → 24,810 |
| 每轮末请求体字节数中位数 | 31,262 → 34,021 | 32,129 → 36,416 |
| 单请求最大模型输入 tokens | 9,327 → 9,869 | 9,298 → 9,760 |

候选多了工具契约，因此首尾单次输入中位数都更大，不能把改善描述为每次输入更省。D1 中 page_observe 从 12 次降至 3 次；候选的 6 次 investigation_check 对应有界调查，不再用多个独立工具往返登记/测量/提交。D2 中 7 次 investigation_check 同时包括健康反例的反驳与异常验证。page_act 仍占多数，且多数前后状态有因果依赖。需要保持语义选择与真实页面反馈，不能无条件将这些动作并行。

本批历史字段最大约 7.6k 字符，候选 D1/D2 分别约 7.0k/7.3k；页面摘要不超过 350 字符，元素不超过 11 个。它们只是这些短路径的观测上限，不证明长页面/长任务永远有界。下一类可推翻选择的证据包括长任务检索失败、复杂布局下语义绑定退化、更快模型在同一质量协议下胜出、以及质量合格的独立分析确实能覆盖串行等待。现有记录不足以宣称调用次数已达理论最低或模型长尾已消失。

## D4：超时状态重放，关闭推理未通过语义门槛

冻结 `6b0e431`，`data/decision-replay/2026-09-24T08-13-31-585Z/`，12 次请求全部完成记录。六份输入、工具 schema、模型和供应商不变，只有 low/disabled 配置变化；无浏览器或工具执行。语义逐条核查及数据哈希在 `semantic-review.json`。

| 指标 | low | disabled |
| --- | --- | --- |
| 有效下一步决策 | 5/6 | 3/6 |
| 六次请求时间合计（包含失败） | 137.549s | 23.842s |
| 已知费用 USD | 0.005255840 + 1 次未知 | 0.006512992 |

disabled 在两个 C2 状态重新调查已由自动规则保存的同一遮挡，缺少新的待验证问题；另一个状态把自动 overlay-blocking 当作 learned rule_check，并用元素 ref 代替触发事件。后者 JSON 结构合法，但领域契约不成立，执行器会拒绝。C0 导航、C5 实际重试目标的连续 actionability 调查、C4 健康恢复反例，两组都提出有效下一步。disabled 的 C5 自由文本使用 permanently，仍超出可证明范围；当前原子工具会生成有限窗口的规范化事实，这不等于认可原模型措辞。

low 在原 C2-2 第 5 请求状态再次超时。新增遥测显示：1.423s 已收到首 reasoning 增量；60s 中断前共收到 953 个事件、12,519 个 reasoning 字符、389,031 字节封装数据，没有任何 tool delta。它直接证明该次失败是持续生成但没有可执行决策，不能解释为一直等不到上游响应，也不能反推原批次未保留部分流的四次请求都属于同一机制。

结论：不采用全局关闭推理，也不把“超时后关闭推理重试”直接加入产品。快但无效的决策不能算提速；本次是状态诊断，不是 12 个完整业务路径。原子工具降低机械步骤有收益，但不能单独解决阻断后的语义收敛。继续探索应改变已有证据与下一步决策的组织方式，或将有类型调查能力接入原生循环做新的公平对照；不再重复相同 low/disabled 重放或仅提高超时上限。


## D5 接入预检：原生循环共享调查能力

原生 quality_investigation 复用产品 createTemporalInvestigator，保留 Stagehand/Browser Use 自身决策循环；同时给原生桥接补齐执行干预来源、未知范围和新旧调查保护。九项真实浏览器测试通过，覆盖同节点复用、替换失效、新窗口、健康反例、谓词差异、未触发要求、无 DOM 变更的网络干预、旧发现保留及并发输入中断。类型检查通过。

首次本地预检因新增测量代码的局部 window 变量遮蔽浏览器全局而失败，记录保留在 data/native-fixture-check/2026-09-24T08-30-07-152Z；已改为 measurementWindow。修正后 data/native-fixture-check/2026-09-24T08-46-22-650Z 四组真实 SDK 预检全部通过：Stagehand 旧链/原子链分别 6/4 次本地模拟模型请求，Browser Use 分别 5/3 次。每组一个实际测量、支持性发现和原生显式结束。全程零付费调用，不是模型质量或速度证据。

预检后用户提出 Jev，核实其能力边界并保留候选。用户随后明确先继续现有实验；下面记录既有模型的完整九轮结果，未调用 Jev。

### D5 九轮结果：共享原子调查仍未解决原生循环的阻断后收敛

冻结 `7983ed2`，批次 `data/oss-compare/2026-09-24T08-51-18-367Z/`，九轮全部实际运行。每个场景/配置仅一次，是方向筛选，不是统计性能结论。全部 127 次请求有费用记录，整批 $0.113840448，没有未知费用或未释放占额。

| 配置 | 通过 | 实际请求 | 报告时间合计，含失败 | 费用 USD |
| --- | --- | --- | --- | --- |
| current-atomic | 3/3 | 24 | 138.923s | 0.033569848 |
| stagehand-atomic | 2/3 | 76 | 380.169s | 0.050664024 |
| browser-use-atomic | 2/3 | 27 | 293.591s | 0.029606576 |

两种原生循环均在 C2 失败。Stagehand 已保存有效遮挡发现，随后继续检查关闭方式、视觉定位、Tab/Escape 和页面提取，最后触及 30 次请求预算，没有主动结束。Browser Use 同样保存了有效发现并提取到没有显式关闭入口；第十次请求 31.358s 后以 `finish_reason=length` 结束，4,096 个完成 tokens 全部记为 reasoning，content 为空，没有可执行动作。SDK 报结构化结果解析失败，但根因证据是输出额度耗尽而未产生决定，不能归类为本项目 JSON 解析器故障。

C5 三组都使用实际恢复按钮完成唯一一次五秒 actionability 测量（26 个 false 样本），保存一个发现、解决对应假设并主动结束，后台各只有一个失败订单。原子能力确实可以共享给开源循环。Browser Use 的目标显示名称仍是 `q8-6`，可读性较差；三组的规范化发现只证明实测窗口，不证明从失败响应起的前五秒，也不认可模型自由叙述中的“永不恢复”。Stagehand 在证据完整后仍额外等待、视觉定位并返回商品页寻找未触发活动；这没有重复下单，但增加了无必要动作。

current 的 C5 第六次请求完成调查，第七次却使用旧元素引用再次调用调查。执行器正确报 `stale-or-unknown-element-ref; observe and rebind`；`agent:response.toolResults` 和下一轮 `latestToolResults.tools` 却都是空数组。该次并未命中调查缓存，不能将唯一测量归因于缓存复用。这是错误回执适配缺失，另在 D6 确定性复现并修复。

224 份产物全部经正式 API 检查；没有执行干预或重复业务写入。停止所有服务后，独立 Python SQLite 只读检查九个数据库的状态、事件 ID/顺序/类型与产物 ID，全部与报告一致，integrity_check 全部为 ok；本批未启用 SQL 观测。记录见 `decision-index.json`、`semantic-review.json`、`durability-audit.json`，附原始文件哈希。它增加了未复现旧持久化异常的证据，仍不解释原根因。

结论：保留当前循环与共享原子调查作为已测范围内的领先候选；不整体切换 Stagehand/Browser Use。质量不相同，不能用上表时间计算“同质量提速百分比”；也不能将相对 B 的变化全部归因于原子工具。更丰富的 DOM、提取和视觉定位没有消除 C2 长尾，下一步优先修复已知反馈缺失，再验证有类型决策与开放探索的分工。正式 minimum 仍为此前的 16/18，不因本次 3/3 升级为发布通过。

## D6：修复失败工具回执，保留模型/工具状态的区别

D5 暴露的错误用实际 Mastra Agent、OpenAI SDK 适配器和本地确定性响应复现：stream 与 generate 两条路径均在工具抛错后返回空 toolResults。Mastra 的 `tool-error` 独立于 `FullOutput.toolResults`；原应用未采集该事件。不是页面定位器吞掉错误，也不是模型未阅读已交付的错误。

请求适配层现采集 `tool-error`，保存调用 ID、名称、参数和明确失败回执，让原有 history/resultRef 与下一轮 decisionMemory 能交付它。模型请求成功与工具失败仍分别记录；工具错误不能被算作有效动作或成功 finish，混合成功/失败调用仍保留真实成功项。工具已开始时不自动重试，取消/超时与费用记录沿用原约束。

回归先确认两个真实 SDK 测试在修复前均失败，再确认修复后错误到达下一轮输入，本地模型的后续读取能正常执行，失败工具不重复执行。此修复不代表真实模型必然正确恢复，也没有证据说明它是 D3 两次 C2 超时的根因。后续真实验证不能把这项正确性修复单独宣称为整体提速。

D6 验证：54 个测试文件、382 项测试通过，typecheck/build 通过。编译后长期服务预检 `data/persistence-preflight/2026-09-24T09-18-43-869Z/` 六轮通过，正式 API 与停服后独立 SQLite 记录一致，零付费请求。新 SDK 测试的后续动作由本地固定响应选择，只证明反馈通路，不作为真实 Agent 自主纠错成绩。

## Jev 官方资料核查与 OpenRouter 连通性更正（2026-09-24）

用户提出 TypeSafe AI System One / Jev。核查官方资料得到一个新的架构候选：让有类型决策模型处理有限、明确的语义判断，生成式模型继续承担开放探索、提出未知假设和生成解释。其潜在价值直指 D4 中持续推理但没有可执行决定的机制；资料并不能证明在本项目有效。

- 官方 [System One](https://docs.typesafe.ai/concepts/system-one) 定义文本/JSON 状态到 Choice、Score、Noul 输出，不能直接输入截图或生成任意字符串；[模型页](https://docs.typesafe.ai/models) 当前稳定版本 jev-1.13.0，按输入收费 $0.042/Mtok，输出免费，state 与最长问题合计最多 32k tokens，总请求 64k。准确性、模型版本与本地网络时延仍需验证。
- 官方 [已知限制](https://docs.typesafe.ai/model-jaggedness/jev-1.13) 明确包含多跳判断、数字计算、无关长上下文和对抗性内容；合法类型不等于语义正确。[confidence](https://docs.typesafe.ai/confidence) 是概率分布的统计量，不能解释为单次决策正确率。
- [Skill suggestion](https://docs.typesafe.ai/cookbooks/skill_suggestion) 与规则候选检索相近；[Function calling](https://docs.typesafe.ai/cookbooks/function_calling) 展示封闭参数空间分派；[Fan-out](https://docs.typesafe.ai/patterns/fan-out) 可借鉴同一状态上独立问题批处理。后续页面状态依赖真实浏览器动作，不能因此并行操作同一页面。
- 官方 [工作流评测](https://evals.typesafe.ai/) 以其他大模型的共识作为参考标签，不能替代本项目的真实 UI/业务 oracle；宣传倍率不是本应用端到端加速证据。[开源对照适配器](https://github.com/typesafe-ai/system-one-adapter-python) 可借鉴同问题接口的普通 LLM 对照，而不是当作 Jev 模型本身开源。

若进入实验，优先用已冻结的真实状态做影子重放，评估证据是否覆盖当前具体问题、候选目标/规则选择与是否需要进一步探索。保留 none/unknown/escalate，不把没有匹配规则解释为没有缺陷。程序负责来源有效性、采样覆盖、预算、写入和 finish 合法性；低置信度、状态缺失或不适合封闭候选时交回 DeepSeek。尤其不能靠过早结束降低耗时。应对比原 Agent、同一决策接口上的 DeepSeek、Jev 三组，以区分模型收益与重构收益；质量合格后才做端到端、陌生业务和正常导航记忆验收。当时尚未调用 Jev；下述更正记录了后续连通性实测，仍没有质量或采用结论。

### 更正：无需额外 TypeSafe key，OpenRouter Decisions API 已可用

用户指出 OpenRouter 的 TypeSafe: Jev 1.13 后，重新核查了[模型页](https://openrouter.ai/typesafe/jev-1.13)及[官方调用说明](https://openrouter.ai/blog/insights/what-is-jev/)。模型 ID 为 `typesafe/jev-1.13`，调用 `POST https://openrouter.ai/api/alpha/decisions`，使用现有 OpenRouter key。不能仅把聊天接口的 model 字段换成 Jev。此次标准 `/api/v1/models` 查询仍未返回 Jev，但专属 `/api/v1/models/typesafe/jev-1.13/endpoints` 返回 text→decisions、32,000 context 与 TypeSafe 提供方；此前凭普通列表判断需要新凭据不充分。

已用现有 key 做一次无浏览器副作用、无重试的合成连通性检查，记录在 `data/jev-connectivity/2026-09-24T09-44-45-890Z/`：HTTP 200，实际模型 `typesafe/jev-1.13-20260917`，提供方 TypeSafe；端到端 1,038ms，433 input tokens，返回费用 $0.000018186。初始商品目录尚未执行购买时，返回 continue，分布 continue=0.98 / finish=0.01 / unknown=0.01。这是单次小输入的接口检查，不是完整任务速度、复杂状态准确率或置信度校准的证明。

下一步无需用户提供新凭据。在 D8 执行集成前，优先把相同的冻结状态与四个结束/探索选项接入 Jev 对照；DeepSeek 对照也使用相同的选择契约，避免将删除自由文本解释的收益全部归因于模型。既有原 Agent 与 D7 记录继续作为独立参考。保留 unknown、全部费用/失败和原始概率，不临时调阈值筛掉错误，不把置信度等同于正确率；质量合格后再选择 D8 的审查模型。

## D7：结束审查影子对比

首次影子批次 `data/completion-replay/2026-09-24T09-26-50-680Z/` 无有效对比结论：原接口请求可执行，但候选六次均 HTTP 404。已中断批次，10 次已发请求、9 条终态账本，其中三次原接口已知费用 $0.0071266；六次 HTTP 错误和中断中的一个请求费用未知，保守占额共 $0.0344736，见 interruption-audit.json。不会拼接重跑后的成功格子。候选同时加入了指定函数 tool_choice 与 parallel_tool_calls=false；公开 Wafer 参数清单支持前者但没有列后者，目前只是兼容性假设。

先补实验网关对流式请求收到非 SSE HTTP 错误的脱敏持久化，并让正式影子批次在配置性 4xx 后停止、SIGINT/SIGTERM 通过取消和账本收尾。增加三请求兼容预检 `completion-replay.ts --transport-check`，同一输入仅比较 forced+parallel / forced 不带 parallel / required 不带 parallel，全部 disabled、无执行、费用上限 $0.10；保留故意无效请求，不作为能力成绩。确认参数原因后再重新冻结完整影子对比。

兼容预检 `data/completion-transport/2026-09-24T09-32-37-364Z/` 完成三次：forced+parallel=false 返回 404，删除 parallel 参数后 forced 与 required 都成功，3.983s/5.041s。因此保留指定单个工具，移除未支持的可选并行参数，并在本地验证必须恰好一个审查结果；不放宽供应商、不静默忽略参数。接下来使用 `completion-review-shadow-2` 新批次完整重跑 30 格；不合并旧批次。这次兼容预检不是语义合格证据。

### D7 修正后的完整影子结果：结束选择改善，但单次审查并不更快

冻结 `e78e859`，`data/completion-replay/2026-09-24T09-33-40-127Z/`，30 次均实际执行并完整记录，费用 $0.050232360，无未知费用。十个状态中七个来自真实记录，三个是明确标注的合成反例。逐条审查、引用核查及原始哈希见 semantic-review.json；所有引用都能在已交付输入中找到，但有引用不代表解释正确。

| 接口 | 请求 / 格式合法 | 五个可结束状态中的结束建议 | 五个须保留探索状态中的过早结束 | 请求时间合计 | 已知费用 USD |
| --- | --- | --- | --- | --- | --- |
| full-low | 10 / 10 | 1/5 | 0/5 | 96.485s | 0.022642728 |
| review-low | 10 / 10 | 5/5 | 0/5 | 116.479s | 0.018641296 |
| review-disabled | 10 / 10 | 1/5 | 0/5 | 20.476s | 0.008948336 |

这里的五个可结束状态是三个 C2 阻断、C4 健康恢复和已完成调查的 C5。原接口没有立即结束不一律意味着错误：它可能先读取详情、尝试正常提交、记录不可达范围或继续验证恢复；表格只比较单次决策是否收敛。review-low 在三个 C2 选择 observed-blocker，在健康与已解决状态选择 scope-covered；在尚未导航、未知异常尚未测量、可见关闭入口、未解决假设和受干预状态继续探索。review-disabled 在三个 C2 仍提出重复调查或没有观察依据的关闭路径，未解决目标问题。

不能报告 review-low “语义 10/10”：其 C5 理由把 status=failed 的处理失败称为 expected rejection，与输入的 canonical businessResult=unknown 矛盾。review-disabled 也有同类错误。结束建议本身可以成立，因为调查已完成、最终业务仍可按 unknown/blocked 报告；但自由文本 basis 不得作为业务事实、发现、规则来源或用户报告。程序必须继续以真实响应、规范化证据与 run_finish 校验生成结论，不能把模型说明当作证据。原子测量的实测窗口也不得被说明文字改写为从响应开始的历史窗口。

这轮不是端到端实验。review-low 的十次孤立决策合计比 full-low 多 19.994s；continue/unknown 后完整 Agent 的费用和时间还没有发生。每轮无条件增加审查很可能更慢，不能据更高结束率宣称提速。下一步只验证选择性接入：已有可结束证据且完整 Agent 出现无进展时，尝试一次审查；无证据、未解假设、待查规则、未完成视觉任务和受干预状态继续原流程。审查不采纳/失败则回到完整探索，全部开销计入原预算。保持显式开关和原子工具默认不变，待真实路径、暖导航及正常恢复反例后再决定是否采用。

当前发布结论仍未改变：D3 正式 minimum 16/18、原持久化异常尚无根因、获准规则的最终复查尚未运行。D5 和 D7 提供了更明确的分工方向，不构成“当前已经最优”或“发布验收完成”的证明。


## D8 前置同契约对照：两种模型均不能获得通用结束权限

冻结 `6ea2c1a`，`data/jev-replay/2026-09-24T09-50-57-535Z/`，20 次全部完成。两组均使用相同完整状态、政策和四选一，不要求生成理由。Jev 十次合计 4.606s（平均 0.461s），DeepSeek 十次合计 282.495s（平均 28.250s）；费用分别 $0.003100272 与 $0.017908304，无未知费用。逐状态审核及源哈希见 semantic-review.json。

Jev 保留全部五个应继续探索/未知状态，但对健康拒绝和已完成异常调查仍选择 continue，结束状态仅 3/5。DeepSeek 五个结束状态均选择结束，却对未解决假设和受干预状态给出 observed-blocker。二者按原门槛都未合格，不能据格式合法或 Jev 速度授予通用结束权限。概率没有作为事后筛选阈值。

下一步是新的、默认关闭的有限候选，并非将这批重新评分成通过：Jev 只审查已有支持性发现、当前检查仍失败且没有可操作恢复控件的阻断状态；原执行器阻止未解决工作、规则/分析待查、干预来源以及状态过期。scope-covered/continue/unknown 都回到原 Agent。这不能处理全部结束问题；完整功能仍由原 Agent 承担，最终仍要求完整 minimum 与获准规则复查，不能用较窄分支取代产品目标。接下来必须做独立真实路径，计入审查及回退的全部耗时/费用。


D8 实现预检：57 文件、413 项测试通过；构建/typecheck/格式通过。真实 HTTP/编译后 Server/Chromium 的首次两次本地预检发现观测后页面版本变化，审查安全跳过（保留 data/atomic-fixture-check/10-10 与 10-12 批次），没有付费调用。新增最多两次重新采集，动态/持续变化页面继续原 Agent，不放松快照一致性。

修正后的 `data/atomic-fixture-check/2026-09-24T10-13-13-080Z/` 七项断言通过：仅一次本地 Decisions 请求，原 run_finish 接受 blocked/unknown，一条原有支持性发现，无业务写入，使用量正确；产物 API 可读，停服后独立 SQLite 状态和完整事件与报告一致。该预检证明集成通路，不证明真实 Jev 质量。最后的重新采集改动另做定向回归后冻结八轮真实诊断。


## D8 真实执行：有限阻断分支有收益，不能宣传为全部路径提速

冻结 `fb4f62b`，`data/oss-compare/2026-09-24T10-14-38-366Z/`，八轮全部实际执行，现有独立验收均通过。两组均启用原子调查，只有 current-review 增加有限 Jev 阻断审查。全部 48 次请求费用 $0.050703538，无未知费用；228 个产物 API 可读。全部服务终结后，用独立 Python SQLite 逐项比较 Run 状态/业务/停止原因、事件 ID/seq/type、产物 ID，八份均一致，integrity_check 均 ok。原 D3 存储异常仍未解释，不能据此称其根因修复。

| 场景 | current-atomic 时间/请求 | current-review 时间/请求 |
| --- | --- | --- |
| C1 可关闭遮罩 | 24.508s / 6 | 21.952s / 6 |
| C2 无关闭控件的遮挡 | 63.968s / 7 | 6.552s / 4 |
| C4 正常业务拒绝 | 21.413s / 6 | 21.152s / 6 |
| C5 未知恢复异常 | 31.393s / 6 | 53.081s / 7 |
| 合计 | 141.282s / 25 | 102.737s / 23 |

已知费用 $0.029775648 → $0.020927890。合计时间减少约 27.3%、请求减少 8%、费用减少约 29.7%，这里只是四对单轮诊断，不估计稳定均值或 P95。真正调用 Jev 的只有 C2：三次正常导航后，Jev 请求耗时 363ms，原执行器重新校验后接受 blocked/unknown，保留同一指针遮挡发现、后台零订单。基线在相同阻断后追加详情读取、失败试点、自由文本覆盖更新及结束，多次模型等待形成差距。

C1 两组均先保存遮挡证据、关闭覆盖层并成功购买；C4 两组均正常拒绝，没有误报重试。C5 两组均保留未知异常调查，一次真实 5s actionability 测量、26 个 false 样本、一条支持性发现和唯一业务提交。C5 新候选没有调用 Jev，反而更慢，多一次完整 Agent 决策；不能将全局长尾或未知问题收敛宣称已解决。没有基于本批结果更改分支门槛或置信度阈值。

手工语义复核发现新阻断分支报告的呈现缺口：pending 条件有保留，但 unexploredBranches 为空，summary 显示零适用缺口。现有 evaluator 未覆盖此呈现要求。采用前修复为执行层在业务结果尚未观测、明确阻断结束时补充未验证的后续路径说明；不生成新事实，不再增加模型调用。此修正也适用于原 Agent 使用相同短结束契约。八轮原始记录保留，不用修正后重跑覆盖原成绩。

当前结论：有限的快速语义审查 + 执行层证据校验能够消除一个真实收尾瓶颈，值得进入发布验证；不替换开放探索 Agent，也不采用 Jev 通用结束权限。默认开关保持关闭。下一门槛仍为覆盖说明回归、持久化一致性的产品保护、正常导航记忆下正式 minimum 18 轮和已获准规则复查。


覆盖说明修正已通过 13 项定向回归、类型检查与构建。修正后的编译集成预检为 `data/atomic-fixture-check/2026-09-24T10-22-48-976Z/`，八项断言通过，正式 API 的 unexploredBranches 明确包含未观测业务结果及待验证条件，仍只需一次本地审查；停服后独立 SQLite 与报告一致。期间一次测试代码类型错误阻止构建，后续误用了旧 dist 的本地预检未通过新覆盖断言；该失败保留，修复测试类型后重新构建才取得上述通过结果。没有付费重跑或覆盖八轮诊断。

### 完成记录一致性保护与独立布局预检

原 D3 持久化异常的根因仍未确定。产品新增两项可检验保护：报告的 run/events/findings/hypotheses/artifacts 从单一只读事务读取；执行最终统计提交后，由新数据库连接核对终态、连续事件、接受结束记录与最后事件身份。缺失/不一致时报告不再声称完成，执行器进入待核查状态并阻止后续业务执行，不自动重复提交。它不证明数据库异常不会发生，也不替代独立停服审计。

完整回归 58 文件/423 项通过；另新增独立预约布局/评估器四项测试，实际浏览器验证不可关闭与可恢复变体及重置，反例覆盖提前结束、缺少物理证据、重复订单和未记录后续范围。定向合计 11 项通过。编译后持续服务预检 `data/persistence-preflight/2026-09-24T10-39-11-445Z/` 六轮，正式 API 与停服独立 SQLite 一致，零付费；`data/atomic-fixture-check/2026-09-24T10-40-00-435Z/` 八项有限审查集成断言全部通过，零付费。

这些是产品保护/集成验证，不是新的模型成绩。接下来按冻结协议执行 12 轮独立布局、18 轮正式 minimum 与六轮获准规则复查，不扩大框架矩阵。
