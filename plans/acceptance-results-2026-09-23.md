# 真实验收诊断：2026-09-23

状态：最新冻结版本 `ae95c91` 的独立诊断 **6/6**、正式验收 **18/18**，M4 自动化门槛通过；M5 未真实验收。下文保留此前全部失败、修复和取消记录，各批不混合统计。

## 最终冻结批次：诊断 6/6，正式 18/18 通过

批次：`data/acceptance/2026-09-23T07-03-41-288Z/`；被测提交 `ae95c91`，工具契约 9；两阶段开始时工作树均干净。运行期间未修改被测构建、模型配置、规则或评分器。构建 SHA-256：`abd12938e7cdaf059459b789558dc6906defdcbd21880390a52f5a077e1a3f19`。

先通过真实 DeepSeek 工具调用和 Qwen 视觉定位 smoke，再完成独立六例诊断，最后串行运行 C0–C5 × 3。正式 `minimum/summary.json` 为 `totalRuns=18, passedRuns=18, gatePassed=true`。此前失败和取消记录保留，不计入本批通过数。

| 案例 | 通过 | 三轮请求数 | 三轮耗时秒 | 验证结果 |
| --- | --- | --- | --- | --- |
| C0 | 3/3 | 8 / 9 / 10 | 26.71 / 33.82 / 38.94 | 正常购买与主动结束 |
| C1 | 3/3 | 12 / 13 / 14 | 80.98 / 128.56 / 69.33 | 遮挡留证、恢复并完成购买 |
| C2 | 3/3 | 10 / 18 / 12 | 169.02 / 204.55 / 106.30 | 不可恢复遮挡，有证据地主动报告阻断 |
| C3 | 3/3 | 10 / 10 / 11 | 29.64 / 25.28 / 24.46 | 按钮改名/移动仍完成购买，无预期外缺陷 |
| C4 | 3/3 | 11 / 11 / 9 | 41.38 / 50.15 / 34.62 | 正确拒付、恢复入口可用，无重复订单 |
| C5 | 3/3 | 11 / 13 / 13 | 58.70 / 55.27 / 58.62 | 无等价预置规则，三轮均自主测量并提交 supported 发现 |

18 次运行均恰好调用一次 `run_finish` 并被接受；没有把系统强停、超时或单纯支付成功转为通过。C0/C1/C3/C4 为 completed，C2/C5 为有依据的 blocked。所有运行遵守 300 秒 / 30 请求 / 40 动作；评分未发现答案泄漏、重复下单、预期外 supported findings 或缺失证据。

正式批次共 **205 次模型请求、1,218,115 输入 token、111,852 输出 token**。网关与执行器计数一致，全部请求成功，usage 无缺失。API 成本约 **$0.113879**。单轮中位耗时 **52.71 秒**，18 轮运行时间合计 **20.61 分钟**；含重置/取证的正式批次约 **21.31 分钟**。C2 为 **106–205 秒**，仍有重复核对，不能宣称效率问题完全解决。

整批含 smoke 和六例诊断共 285 次请求，API 成本约 $0.159741。不含开发助手用量、此前调试批次以及旧取消/超时请求的未知 usage。正式 205 次主模型请求实际全部路由到 Wafer；策略仍是 OpenRouter auto，诊断还出现 Relace，视觉 smoke 为 Alibaba。结论适用于本次配置和实际供应商，不能推导跨供应商同样可靠，也不能将各批速度差全部归因于代码。

30 个测试文件、228 项测试、构建和类型检查通过。真实浏览器测试覆盖遮挡测量及无点击/无滚动副作用；游标测试使用实际交付给 Agent 的引用续读；执行器测试覆盖恢复后持久发现和业务事实。服务、靶场及两个控制/业务端口均已停止，数据库和证据保留。

**M0–M4 在当前购买靶场的自动化门槛通过；M5 尚未真实验收。** M5 还需发现的人工确认、模型生成候选、正反例与 unknown 验证、人工审阅批准、启用后正常/异常复查及成本比较。此次未代替人确认或发布规则，未启用学习规则污染 C5 未知发现验收。评分由本项目实现，仍需人工抽查，不等于独立第三方认证。通用业务覆盖、通用写权限、流式首响应/idle 指标不在此次已验证范围内。

下一步完成 M5，并以新业务靶场验证泛化；性能优化需固定供应商另做对照，不削减证据或放宽完成标准。

## 首批失败诊断的配置与证据

- 被测执行层：`2fea585`，本批次不修改执行器、提示词、规则或预算。
- 私有诊断入口复用正式 Run API、C0–C5 重置/独立证据和同一个 evaluator；每例一次，不混入 minimum 的六例各三次。
- 模型：`deepseek/deepseek-v4.1-flash` 与 `qwen/qwen3.7-plus`，OpenRouter 自动路由、禁止替代模型和 provider fallback。
- 每例固定 300 秒 / 30 请求 / 40 动作；请求 60 秒、最多一次安全重试、工具 15 秒、收尾最多两次主模型请求。
- 运行目录：`data/acceptance/2026-09-23T05-31-05-071Z/`。包含模型清单、构建哈希、代码差异、逐请求账单、原始请求/响应、正式报告、独立证据和评分；不提交密钥及原始运行数据。
- 真实 DeepSeek 工具调用与 Qwen 视觉定位 smoke 已通过。

## 首批单次诊断结果（历史）

| 案例 | 完整评分 | 模型请求 | 耗时秒 | 实际行为 |
| --- | --- | --- | --- | --- |
| C0 | fail | 17 | 100.95 | 支付成功；离开结果页后变为 unknown，反复读取历史，未结束 |
| C1 | fail | 11 | 51.33 | 规则检出遮挡并留证；未关闭遮罩继续购买，未结束 |
| C2 | fail | 11 | 41.49 | 规则检出不可关闭遮挡并留证；未主动报告阻断并结束 |
| C3 | fail | 18 | 139.84 | 找到改名/移动的按钮并支付成功；离开结果页后结束校验被拒 |
| C4 | fail | 15 | 117.32 | 正确识别拒付后主动重试，产生第二笔订单；随后未结束 |
| C5 | fail | 16 | 84.69 | 两次持续测量，但未提交 supported 发现；未知规则发现失败 |

六次均以 `blocked/no-progress` 停止；C1/C2 的 findings 已由独立证据验证，但系统强制停止不计为完整 pass。C5 的两次测量约 12 秒和 6 秒，均观察到不可操作，仍未产生已验证发现。C3 唯一一次成功结束请求被当前页业务校验拒绝，其余五例没有调用 `run_finish`。

六次总计 **88 次请求、415,480 输入 token、35,920 输出 token**。网关计数与执行器一致，usage 无缺失；六次加 smoke 共 90 次请求，网关报告合计约 **$0.036170**。这是模型 API 账单，不包含开发助手用量。

所有案例均在原预算内，独立评分未发现答案泄漏；本批未出现上游请求失败或超时重试。C4 存在第二笔业务订单，不能据“模型调用没有重试”宣称业务写约束通过。当前没有未预期的 supported finding，不等于全面证明不存在误报。

真实路径结果与 smoke/确定性检查分开：28 个测试文件、208 项测试、类型与格式检查通过；并不能覆盖本次发现的跨轮工具结果问题。

## 首批结论与当时的修复计划（历史）

- M2：简单购买操作曾通过，但完整检查目标下的可靠收尾未通过。
- M3：C1/C2 遮挡规则与证据在本次均工作，完整任务闭环未通过。
- M4：六例单次诊断失败，18 轮正式验收未运行，不能宣布完成。
- M5：C5 尚无 supported 探索发现，无法开始真实的“确认 → 生成 → 验证 → 人工批准 → 启用 → 复查”链路；未伪造反馈或批准来绕过前置条件。

优先修复按需工具结果的交付和超长结果的摘要，再修复业务事实保存、条件适用性及业务写约束。先用本批真实轨迹补下一轮输入契约回归，之后另起冻结批次跑六例；六例全过才进入固定 18 轮。不要增加上下文总预算或恢复整页输入作为唯一修复，也不要用更早强停替代成功收尾。

## 首批确认的失败机制

### 1. 按需历史读取结果没有交到下一轮模型

C0 的 `history_read` 正常返回 `total/start/entries`；下一次实际模型请求只保留 `tool` 和 `args`，缺少返回的历史正文。`compact-history.ts` 的结果投影没有处理 `entries`。由于每次 `generate` 只执行一个 step，工具结果需要显式进入下一轮请求；工具执行成功并不表示模型已经看到了结果。

证据：C0 的第 13 次网关请求包含读历史的参数，但没有所读内容，随后多次重复读取。修复应保证按需获取的内容至少完整地进入下一次决策，并限制历史展开深度、分页和字节上限，避免递归复制。

### 2. 单条结果过大导致近期记忆全部为空

C1 第五轮的 `element_details + checks_run` 经投影仍有 13,308 UTF-8 字节，超过 8,000 字节历史预算。当前压缩器遇到最新条目超限立即退出，输出空数组，连最新结果也未保留。实际第 6–10 次模型请求的 history 都为空，随后反复查询相同细节。遮挡规则已经发现问题，但 Agent 没有恢复并继续购买。

修复应对工具结果分别提供有意义的小摘要、证据引用与按需展开；必须保证最新动作/查询的结果可见，不能依赖扩大预算或直接丢空历史。

### 3. 离开结果页会覆盖已经验证的业务结果

C0/C3 均实际支付成功。继续导航后，`observe()` 因当前页面没有订单反馈，把 `businessResult` 从 success 改回 unknown。C3 的结束请求因此被 `outcome-not-supported` 拒绝。当前页面状态应与已经有订单/可见证据的业务结果分开保存；离开结果页本身不是结果失效的证据。

### 4. 条件性要求缺少适用性状态

C0 在观察到正常支付前，就把“支付失败时能否重试”登记为开放假设。成功路径不会触发这个条件，Agent 随后继续寻找未发生的分支。完成协议需要区分本次适用/未触发/确实未验证的条件，要求范围内已触发的调查完成；不能把所有未出现的分支一概当成阻断，也不能仅凭支付成功就结束全部检查。

### 5. 恢复验证产生了第二笔订单

C4 在正确观察拒付结果后，Agent 主动点击 `Try Again`；后端记录两笔 rejected 订单。它不是 SDK 重试或旧尝试迟到，而是一次新的正常工具调用，因此不能用请求重试保护来解决。当前验收要求单笔订单，恢复检查需要明确区分“入口可操作性验证”和“再次业务提交”；必须把允许的业务写次数/对象与恢复语义纳入执行契约，不能单靠“不要重复购买”的提示。

本批次用于验证真实行为。确定性测试通过，不代表上述完整交互链已覆盖；此次未用调整阈值或重跑挑选成功结果掩盖失败。


## 工具结果交付修复后的第一批复验

批次：`data/acceptance/2026-09-23T05-56-01-908Z/`，被测代码 `f8a53a7`，工具契约 5。模型、供应商策略、300 秒 / 30 请求 / 40 动作及正式评分器不变，开始时工作树干净。该批次不与前一批合并统计。

| 案例 | 完整评分 | 请求 | 耗时秒 | 停止原因 |
| --- | --- | --- | --- | --- |
| C0 | fail | 15 | 81.95 | blocked |
| C1 | fail | 12 | 72.17 | blocked |
| C2 | pass | 10 | 83.89 | blocked |
| C3 | fail | 11 | 75.86 | blocked |
| C4 | pass | 11 | 74.42 | goal-reached |
| C5 | fail | 19 | 195.08 | no-progress |

结果 **2/6**。C2 主动报告不可关闭遮挡后结束，C4 正确识别拒付并以无提交操作验证恢复，无第二笔订单。C0/C1/C3 都已经成功购买、主动请求结束；其中 C1 完成了遮挡留证 → 关闭 → 付款。它们仍失败的原因是 `exploration_update` 的自由文本未覆盖分支被服务端一概当成缺项，导致正确的结束请求被拒，然后模型改报 blocked。

C5 首次提交了有完整持续测量证据的 supported 发现，独立 findings 检查通过；但将 `status=failed` 的可重试处理失败错误映射为 `businessResult=rejected`。结束接口未给出明确允许值，模型重复确认页面，最终 no-progress。证据与失败状态全部保留，未计为完整通过。

该批次证明了工具结果交付、遮挡恢复、业务事实保存和单订单限制的改善，也暴露了剩余的分支/结束契约问题。没有进入 18 轮或 M5。

后续修复将未覆盖分支改为 `{description, trigger}`，由实际业务/页面事实计算适用性；结束拒绝返回 `finishAdvice`，明确已验证的业务分类及缺项。已通过的支付或拒付且没有适用缺项时，不能以“未触发条件”为由宣称 blocked；系统要求 Agent 更正结束请求，而不会自动改写为成功。


## 结构化分支与结束反馈修复后的复验

批次：`data/acceptance/2026-09-23T06-10-28-504Z/`，被测代码 `896504c`，工具契约 6。同样的模型、预算和评分器，六例单次全部通过。

| 案例 | 完整评分 | 请求 | 耗时秒 | 停止原因 |
| --- | --- | --- | --- | --- |
| C0 | pass | 6 | 27.74 | goal-reached |
| C1 | pass | 10 | 66.38 | goal-reached |
| C2 | pass | 18 | 261.66 | blocked |
| C3 | pass | 8 | 34.60 | goal-reached |
| C4 | pass | 12 | 126.46 | goal-reached |
| C5 | pass | 15 | 104.51 | blocked |

C1 保留遮挡发现后恢复并购买，C2 有证据地报告不可恢复阻断，C4 无重复订单，C5 自主测量并提交 supported 发现后主动结束。支付成功、拒付、可重试失败的业务分类与检查结果分开，未触发分支不再阻止合法结束。

但 C2 仍暴露并行读取边界：三份原文结果分别被等分预算再次省略，模型读到新的引用，产生递归读取；省略 offset 时还可能出现无效游标。因此暂停进入稳定性结论，修复为源头限页、显式默认游标、每轮最多三次读取及读取正文优先交付，共享上下文预算仍为 8,000 字节。

运行器已自动开始的 minimum C0 被通过正式 API 取消，记录在 `operator-cancelled-run-739233b1-c331-4651-a68a-1dae37a1ac25.json` 和 `operator-stop.json`。保留该中止任务；取消时有一笔网关请求尚未结算，usage 未知，不把它当作零成本。该正式批次不完整，不能计为 18 轮验收。后续使用修复后冻结版本另起完整批次。


## 工具契约 7 复验：5/6，暴露续读引用混淆

批次：`data/acceptance/2026-09-23T06-27-43-334Z/`，被测代码 `604f476`。运行的是已冻结构建；随后开发的契约 8 未装入本批服务。预算、模型和评分器不变。

| 案例 | 完整评分 | 请求 | 耗时秒 | 停止原因 |
| --- | --- | --- | --- | --- |
| C0 | pass | 10 | 98.22 | goal-reached |
| C1 | pass | 11 | 72.65 | goal-reached |
| C2 | fail | 12 | 300.05 | budget-exhausted |
| C3 | pass | 17 | 150.25 | goal-reached |
| C4 | pass | 12 | 135.28 | goal-reached |
| C5 | pass | 14 | 110.53 | blocked |

C2 的遮挡发现及原始证据均通过，但任务最终超时，不能算完整 pass。一笔模型请求在 60 秒截止，受限重试随后恢复；后续还出现续读错误：原始 `4.1` 的第一页返回 `nextOffset=916`，进入下一轮时原始引用被外层历史编号 `6.0` 覆盖，模型因此续读了读取回执自身。新的分页正文交付已生效，但引用身份的契约仍有缺陷。

契约 8 将原文 `resultRef` 与回执 `receiptRef` 分开。回归不再用测试自身保存的源引用续读，而是使用实际发给模型的引用和游标，逐页重建原始 JSON；另覆盖经 history_read 再取得游标。226 项测试通过；短回执补充类型字段后，相关测试和类型检查再次通过。未改变结果评分与预算。C5 本批完成自主假设、六秒测量、supported 提交和主动结束。

本批因诊断失败没有开始正式 18 轮，也没有开始 M5。所有六例已保存；不会与其他批次挑选合并。


## 工具契约 8 复验：5/6，发现可操作性测量矛盾

批次：`data/acceptance/2026-09-23T06-44-20-144Z/`，被测构建 `ae07060`。同样的冻结模型、预算和评分器。运行中另行开发的契约 9 未载入本批服务。

| 案例 | 完整评分 | 请求 | 耗时秒 | 停止原因 |
| --- | --- | --- | --- | --- |
| C0 | pass | 8 | 77.98 | goal-reached |
| C1 | pass | 18 | 222.75 | goal-reached |
| C2 | fail | 11 | 300.05 | budget-exhausted |
| C3 | pass | 9 | 99.08 | goal-reached |
| C4 | pass | 13 | 196.75 | goal-reached |
| C5 | pass | 16 | 156.71 | blocked |

分页引用已分离。本批 C1 支付后为总结遮挡经过读取多页历史，虽然主动完成，但花费 18 次请求；当前输入的 submittedFindings 只有 ID，缺少问题内容。后续补最多 2,000 字节的持久发现摘要并标注省略数量，避免依赖逐页历史回放。工具回执与近期历史仍共享 8,000 字节；未恢复完整页面输入。

C2 的完整评分仍失败。其 transition_observe 报告 `element-actionable` 的 48 个样本全部为 true，而 DOM 遮挡规则和真实点击探测均显示不可点击。代码将 visible && enabled 等同于 actionable，遗漏命中检测。此矛盾是本批可确认的执行层缺陷；不能把超时全部归因于模型不服从或断言它是唯一原因。本批模型等待时间也较高。

契约 9 的修复使用只读视口命中采样：可见、enabled，且至少一个采样点命中目标或其后代。缺失和歧义为 unknown；采样不点击、不主动滚动，也不保证业务事件处理成功。真实浏览器测试覆盖遮挡前后、disabled、视口外、歧义及无副作用；执行器测试验证同一遮挡测量为 false，恢复后下一轮仍有 finding 摘要。228 项测试及类型检查通过。

本批六例完整保留，C0/C1/C3/C4/C5 通过，C2 超时；18 轮和 M5 未开始。OpenRouter 自动路由供应商可能变化，逐请求记录保留；不把各批延迟差直接解释为代码改动的因果效果。
