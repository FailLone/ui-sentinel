# M5 真实学习闭环验证

状态：绑定式执行修复后，冻结代码 `af85312` 的六轮真实 Agent 复查 **6/6 通过**，当前购买靶场的 M5 学习闭环通过。两批历史 5/6 均保留；源 M4 批次未修改，原有冻结版本 18/18 结论保留。本结论不代表跨业务泛化或稳定提速已经实现。

## 来源与人工确认

- M4 冻结批次：`data/acceptance/2026-09-23T07-03-41-288Z/`，正式 18/18 通过。
- 使用 C5 第三轮 `run-06b5fa59-aeb3-446d-8e34-c68f229aaa3f` 的 `finding-7d1433e4-4ce6-4099-80d6-814d38d5161b`。
- 该条记录包含完整五秒的 21 个不可操作样本，目标名为语义性的 `Retry button`。选择它是为了保留可迁移的目标含义；另外两轮的同类发现与成本仍保留，不以此挑选最好表现。
- 用户已明确回复“确认，生成规则候选”；此确认只针对缺陷，不是规则启用批准。
- 在关闭的 M4 数据库副本中记录反馈与学习结果，原始数据库和评估记录不修改。

## 验证方法

1. 从真实发现关联的测量事实生成声明式候选；只允许既有事件、条件、语义目标和受限状态过滤，不生成任意代码。
2. 新增私有正常恢复对照：同样返回 `status=failed, canRetry=true`，但重试入口可操作。默认 C5 仍不可操作；正常 reset 清除 override。该开关不进入公开变体配置或 Agent 任务。
3. 用浏览器实际操作、trial 点击、命中采样和截图采集正常对照，不能用改写 false 为 true 的方式伪造正常证据。
4. 原始异常测量应 fail、真实正常对照应 pass、缺失事实应 unknown。候选和验证结果展示给人后才可批准。
5. 批准后在独立环境启用，异常和正常各三轮复查；保存全部运行、模型账单、截图与原始测量。检查规则结果、主动结束、单订单约束和证据可用性，并比较探索与复查成本。

模型仍为 DeepSeek + Qwen；固定主模型供应商 Wafer，与已通过 M4 正式批次的实际主模型供应商一致。生成候选最多一次请求、60 秒硬截止，provider 忽略取消也不能无限等待或迟到后写入候选。复查每轮仍为 300 秒 / 30 请求 / 40 动作。不得改写候选以使测试通过；失败保留并明确归因。

## 入口

`pnpm experiment:learning` 的准备模式要求 source、finding 和明确的人工确认原因；准备完成后退出并关闭服务。恢复模式要求原学习目录、精确 proposal ID 和人工 reviewer，仅用于已经得到该候选明确批准后的启用与复查。没有批准不得执行恢复模式。

本流程复用正式反馈、候选生成、验证、审阅、启用与 Run API。正常 fixture 的浏览器采样是私有评估控制，不属于被测 Agent 能力。候选验证不等于启用后复查，也不等于 M5 已通过。

## 首次候选：验证拦截了错误适用范围

记录：`data/learning/2026-09-23T07-54-51-016Z/`，候选 `proposal-49d3f5c1-4d9f-4f1a-8d93-235e8e894431`。真实模型一次调用，Wafer，24.58 秒，输入 872 / 输出 1,904 tokens，费用 $0.001263608。

异常输入返回 fail、缺失事实返回 unknown，均符合预期；真实正常恢复对照返回 unknown，未达到 pass。候选错误地把 `fromState=retrying-disabled` 作为适用条件，将健康恢复排除在外。正常对照的 27 个实际浏览器采样均可操作，不能为了让测试通过而改写其状态。该候选未批准、未启用。

检查实际发出的模型请求发现，结构化输出适配把可选状态字段转成必填字符串，使模型无法省略状态限制。修复生成协议，显式允许 `null` 表示不限制，存储时规范化为字段缺省；同时支持把前一候选及其验证失败作为下一次生成的输入。旧候选和测量事实保持不变，新候选需要重新验证。

修订入口：`pnpm experiment:learning -- --revise <上次学习目录> --previous <失败候选 ID>`。它复制已关闭的学习数据库到新目录，复用已有人工确认，重新采集正常对照并调用真实模型；仍不批准、不启用。

当前计时以 transition 测量开始为原点，不代表已经实现支付响应时刻到入口恢复时刻的完整后台追踪。C5 的持续不可操作事实得到验证，但不能据此宣称任意业务的精确恢复 SLA 已可验证。

## 修订候选：待人工审阅

- 冻结代码：`c1087a3`；构建、格式检查及 234 项测试通过。
- 记录：`data/learning/2026-09-23T08-02-16-954Z/`。
- 候选：`proposal-e7599b50-0dd0-429f-9cae-b7bc7768f7ae`，来自同一已确认 finding，保留前一候选与验证反馈关联。
- DeepSeek 真实生成一次，供应商 Wafer，10.23 秒，输入 2,388 / 输出 1,368 tokens，费用 $0.001152732。两次生成合计 $0.002416340；不包括未来启用后复查。
- 实际发送的 JSON Schema 已允许状态字段为 null；模型选择不限制初始状态，保留目标状态 `retry-actionable`。
- 原始异常返回 fail，重新采集的真实正常恢复返回 pass，缺失事实返回 unknown，全部符合预期。
- 正常对照 27 个采样均可操作，包含截图与原始测量；M4 源数据库哈希确认未改变。

实际候选配置（未经人工改写）：

```json
{
  "type": "transition",
  "name": "retryable-failure retry-actionable within 5 seconds",
  "trigger": {
    "eventType": "retryable-failure",
    "toState": "retry-actionable"
  },
  "expectation": {
    "condition": "element-actionable",
    "target": "Retry button",
    "timeoutMs": 5000
  },
  "severity": "error"
}
```

完整模型描述、配置及验证输入见该目录的 `prepared.json`。`Retry button` 是测量目标的语义名称，不要求页面按钮使用固定文案或 ID。触发事件和目标状态仍需 Agent/采集器正确标记；此次静态事实验证尚不能证明启用后的 Agent 复查稳定性。

该候选尚未批准。用户随后提出“任何重试类别的按钮”应复用同一规则，转入新修订；下面命令仅保留为历史记录，不执行旧候选启用：

```sh
EXPERIMENT_AGENT_PROVIDER=Wafer pnpm experiment:learning -- \
  --resume data/learning/2026-09-23T08-02-16-954Z \
  --approve proposal-e7599b50-0dd0-429f-9cae-b7bc7768f7ae \
  --reviewer user
```

## 人工审阅：推广到所有重试类别

用户原文：“我觉得，规则需要再通用一下，不止是重试支付，任何重试类别的按钮，都可以走这条规则。”这是修订范围的要求，不是对先前候选的启用批准。

将规则定义为“重试入口可用性”：来源案例是支付，但规则适用于有业务证据确认当前可重试的任意失败操作。已有事件 `retryable-failure` 与目标 `Retry button` 本身不绑定支付，不需要按业务复制 evaluator。保留可配置的等待窗口，当前候选沿用 5 秒与 error；冷却、次数耗尽、处理中等状态不能仅凭按钮文案被判为可重试。

本轮补充审阅反馈到候选生成 API/实验入口，并保存反馈与前一候选关联。规则层测试包含支付、上传、加载、同步，以及等待窗口、非重试目标、资格不匹配事件。后几类为明确标注的合成契约测试，不冒充真实业务或 Agent 的泛化验收。真实端到端来源目前仍只有支付。

详细适用边界及尚未实现的资格/多目标保障见 [规则库设计](../docs/rules-and-rule-library.md#13-示例跨业务的重试入口可用性)。

首次泛化生成记录：`data/learning/2026-09-23T08-21-00-574Z/`，代码 `ec95fcc`（245 项测试通过）。模型将已记录的语义键 `Retry button` 改成 `Retry control`，被事实匹配校验拒绝，未创建候选、更未启用。一次 Wafer 请求 21.90 秒，输入 2,484 / 输出 3,768 tokens，费用 $0.002606076。失败的模型响应和全部测量仍保留。

这不要求增加一个支付专用规则；需要将已有事实语义键作为生成协议的枚举值，而不是自由文案。修复后模型可修改名称和适用说明，但 target 必须引用已观测语义键，不能为表达泛化而发明新引用。

## 通用候选：生成与验证结果

- 代码：`bb684e9`。前一步全套 245 项测试通过；最终语义键枚举修复另跑相关 27 项测试通过，构建与格式检查通过。
- 记录：`data/learning/2026-09-23T08-22-47-503Z/`。
- 精确候选：`proposal-fc30e9bb-46bc-40ec-b88b-ff52f0565607`。
- 单次真实生成：DeepSeek / Wafer，24.69 秒，输入 2,484 / 输出 3,175 tokens，费用 $0.002068260。连同本轮被拒绝的目标改名请求，泛化修订生成总费用 $0.004674336。
- 模型自行将 `fromState` 和 `toState` 均设为 null，存储时规范化为不限制状态；目标仍为已有语义键 `Retry button`，名称和描述明确适用于任意重试类别。
- 原始异常 fail、重新采集的真实正常恢复 pass、缺失事实 unknown，全部符合预期。源 M4 数据库哈希未改变。
- 使用**实际生成的候选配置**补验 20 项合成输入：支付/上传/加载/同步各自的持续不可操作、立即可操作、3 秒后恢复、事实缺失，共 16 项；冷却、次数耗尽、处理中、资格未知的非匹配事件共 4 项。全部符合预期，完整输入和结果保存在 `generic-contract-validation.json`。
- 合成测试只证明在语义事件已正确分类、目标已正确绑定的前提下，规则无需按业务复制。不能证明 Agent 已能正确处理非支付业务、重试资格或多目标覆盖。

实际候选关键配置：

```json
{
  "type": "transition",
  "name": "retryable failure offers operable retry within configurable timeout",
  "trigger": { "eventType": "retryable-failure" },
  "expectation": {
    "condition": "element-actionable",
    "target": "Retry button",
    "timeoutMs": 5000
  },
  "severity": "error"
}
```

完整模型描述及验证输入保存在该目录的 `prepared.json`，明确区分预期跨业务适用范围和当前已验证的支付案例。生成时的用户要求只是范围修订，故当时没有执行 review approve 或 enable；后续授权和真实复查结果见下节。

本次获得后续授权后，使用同一冻结构建执行的恢复命令（已完成，不在原目录重复运行）：

```sh
EXPERIMENT_AGENT_PROVIDER=Wafer pnpm experiment:learning -- \
  --resume data/learning/2026-09-23T08-22-47-503Z \
  --approve proposal-fc30e9bb-46bc-40ec-b88b-ff52f0565607 \
  --reviewer user
```

## 启用后真实复查：5/6，未达到 M5 门槛

用户在审阅通用候选后回复“下一步怎么说？开始复查？”，据此启动此前约定的隔离环境启用与六轮复查，授权范围保存于 `human-review.json`；正式 API 的批准记录为 `approval.json`。使用候选 `proposal-fc30e9bb-46bc-40ec-b88b-ff52f0565607`，构建哈希与准备阶段一致。仅隔离副本启用，M4 源数据库哈希未变。

全部记录仍在 `data/learning/2026-09-23T08-22-47-503Z/`。原始评分见 `recheck-summary.json`，成本、目标、证据和错误分析见 `recheck-analysis.json`；每轮独立 JSON 与证据目录均保留。

| 场景 | 轮次 | 验收 | 模型请求 | 耗时 | 学习规则结果 | API 费用 |
| --- | --- | --- | --- | --- | --- | --- |
| 异常 | 1 | pass | 18 | 77.66 秒 | fail | $0.015865 |
| 异常 | 2 | pass | 13 | 76.22 秒 | fail | $0.011771 |
| 异常 | 3 | pass | 12 | 55.27 秒 | fail | $0.010866 |
| 正常恢复 | 1 | pass | 12 | 45.02 秒 | pass | $0.009672 |
| 正常恢复 | 2 | pass | 11 | 44.59 秒 | pass | $0.009002 |
| 正常恢复 | 3 | **fail** | 12 | 42.23 秒 | **unknown** | $0.009629 |

- 六轮均主动调用 `run_finish` 并被接受，无超时、重复下单或模型请求失败；每轮一个订单。运行状态均为 blocked，因为支付处理失败仍映射为业务结果 unknown，单订单策略不允许再次付款；这与正常恢复入口本身是否健康分开判断。
- 异常 3/3 产生 supported 规则发现；正常 3/3 均没有 supported 缺陷误报，但第三轮没有取得所需的规则 pass，不能把“没有误报”等同于完整通过。
- 合计 78 次 DeepSeek 请求，实际供应商全部 Wafer，usage 完整；503,488 输入 / 41,267 输出 tokens，API 费用 $0.066803972。累计运行时间 340.997 秒，约 5.68 分钟，不包括此前候选生成费用。配置仍支持 Qwen，但本批未触发视觉模型调用。
- 242 个报告证据项全部可下载且通过文件校验；额外查看异常第三轮和正常第一轮截图，与不可操作/可操作测量一致。所有本轮服务已关闭。

### 失败原因：规则引用与页面目标名称脱节

正常第三轮 `run-98fee15e-131c-4294-958d-2b8d6fedae81` 的 Agent 测量了正确的页面按钮，20 个 actionability 样本全部为 true，也正确将自己的缺陷假设 refuted。但它把测量目标命名为 `Try Again button`，规则引用的语义键是 `Retry button`。规则按事实引用匹配，因目标不一致始终返回 unknown。

这是执行阶段的目标绑定缺陷，不是按钮失效、模型超时或规则误报。生成阶段的枚举约束已经防止候选改名，但 `transition_observe` 的 target 仍由 Agent 自由输入，仅有提示词要求沿用规则语义，未形成代码约束。没有改写测量事实、把 unknown 改成 pass，或重跑某个单轮来拼凑成功批次。

### 效率与重复报告

同类异常的首次探索三轮合计 37 次请求、172.595 秒、221,499 输入 / 15,301 输出 tokens，费用 $0.017681333。本次启用规则后的异常三轮为 43 次、209.158 秒、279,263 输入 / 24,394 输出 tokens，费用 $0.038500949。请求增加约 16%，耗时增加约 21%，本批没有证明提速或节省 token。

这是小样本的历史对照，不是随机并发 A/B；供应商缓存比例、模型生成行为及实际账单差异都保留，不能把费用差异全部归因于规则。正常恢复没有同条件的首次探索对照，不和异常探索混算收益。

三个异常轮次均由规则保存一次发现后，Agent 又提交了同一个问题，形成两份 supported 记录。其中第 1、3 轮还发生无效证据引用，被执行器拒绝后重试成功；第 2 轮测量了约 12 秒，超过声明的 5 秒所需窗口。这些都是额外开销，尽管未造成此次异常验收失败，也必须保留。

### 下一步修复顺序

1. 为已知规则增加绑定式测量入口：Agent 选择规则与当前页面元素；执行器从已批准声明派生 eventType、语义 target、condition 和等待预算，拒绝矛盾参数。内部 rule ID 用于引用规则，不是要求业务页面提供 DOM ID。新问题探索仍保留自由事实采集入口。
2. 将规则结果、证据引用和关联检查的完成状态一并交付 Agent；让已匹配的规则检查直接关闭对应检查事项，避免再次按“未知缺陷”提交。重复合并必须绑定同一操作/目标与证据，不能仅凭标题相似合并不同问题。
3. 增加文案变化、目标绑定失败、正常入口、合法例外及缺失事实测试；冻结修复后在新记录目录完整运行异常/正常各三轮，保留本次 5/6 失败批次。规则内容未变化时，不需要再次要求用户批准同一条规则。

上述为首次复查后的问题与修复顺序，后续实施及新批次见下节，原始 5/6 记录不变。

## 绑定式检查修复与新批次

用户随后明确要求开始实施。新增工具契约 10 的 `rule_check`，由 Agent 选择当前页面元素和触发证据，由执行器派生测量参数、保存逐检查结果、复用已匹配发现并关闭显式关联假设。当前资格适配器限于已记录的购物业务协议，其他业务保持 unsupported/unknown；不会凭借规则名或按钮文案制造资格。

确定性验证已覆盖实际浏览器中不同文案、多个按钮、过期引用、正确语义键、显式假设关闭与重复结果复用；业务资格还覆盖冷却、次数耗尽、处理中、前置条件不满足及跨运行/旧事件引用。完整测试 261 项通过，随后新增多目标 unknown 汇总检查并复验相关测试；构建通过。新批次执行更严格的 `bound-rule-1` 验收，不改变已批准声明或历史记录。

### 工具契约 10 首批：5/6，保留失败

代码 `08d568c`，目录 `data/learning/2026-09-23T08-53-46-406Z/`。六轮都正确完成绑定检查；正常 3/3 取得规则 pass，原来的语义目标错配未复现。但异常第一轮出现两个规则发现，新增的去重门槛未通过，故整体仍是 5/6。

该轮模型把可选 hypothesisId 填成字符串 `"null"`，校验拒绝后先走自由测量，再走绑定检查；旧的运行期 evaluator 允许两种事实来源各自产生规则发现。其余五轮全部通过包括去重在内的门槛。六轮合计 65 次模型请求、263.348 秒，费用 $0.051692170，全部记录保留，不与下一批合并。

工具契约 11 将假设关联改成明确的 `hypothesisIds: []` 或 `[已有 ID]`，不再让模型填写可选字符串；同时要求已启用学习规则只消费其自身绑定事实。原始候选与人工批准均不修改。已知规则的正反 fixture 验证仍调用通用 evaluator，不依赖运行期绑定。

### 工具契约 11 最终批次：6/6

代码 `af85312`，目录 `data/learning/2026-09-23T09-00-54-689Z/`。263 项确定性测试、真实 SDK 工具参数契约测试、类型检查、构建及格式检查通过。新目录复制已关闭的原学习数据库，核对候选内容完全一致，继承已有人工批准；没有再次生成或改写规则。

| 场景 | 轮次 | 验收 | 模型请求 | 耗时 | 绑定结果 | supported 发现数 |
| --- | --- | --- | --- | --- | --- | --- |
| 异常 | 1 | pass | 9 | 45.64 秒 | fail | 1，规则来源 |
| 异常 | 2 | pass | 13 | 77.87 秒 | fail | 1，规则来源 |
| 异常 | 3 | pass | 11 | 49.02 秒 | fail | 1，规则来源 |
| 正常恢复 | 1 | pass | 8 | 42.17 秒 | pass | 0 |
| 正常恢复 | 2 | pass | 8 | 35.96 秒 | pass | 0 |
| 正常恢复 | 3 | pass | 10 | 71.50 秒 | pass | 0 |

- 六轮均只有一次完成的绑定检查，使用规则的五秒窗口；无重复发现、无无效证据引用、无正常缺陷误报。全部主动结束、每轮一个订单、未超时。
- 215 个证据项可下载并通过文件校验；检查异常第一轮和正常第一轮的绑定说明、截图与测量，分别正确关联 `Retrying...` 和 `Try Again`，两者均使用规则语义键 `Retry button`。
- 59 次模型请求全部成功，实际均为 DeepSeek / Wafer，usage 完整；385,418 输入 / 32,802 输出 tokens，累计运行 322.167 秒（5.37 分钟），费用 $0.050124758。此次未调用视觉模型。
- 本次修复过程两批真实复查合计费用 $0.101816928；候选生成与更早批次另行保留，不混合计算最终通过率。
- 源 M4 数据库和最初 5/6 学习数据库哈希均未改变，所有本批服务已关闭。汇总见 `recheck-summary.json`，完整分析与工具错误见 `recheck-analysis.json`。

相对修复前相同六场景批次：请求 78 → 59（减少 24%），输入 token 503,488 → 385,418（减少 23%），输出 token 41,267 → 32,802（减少 21%），费用 $0.066804 → $0.050125（减少约 25%）。累计耗时 340.997 → 322.167 秒，只减少约 6%；模型延迟与观察次数仍有波动。

相对最初无等价规则的 C5 探索三轮：异常复查请求 37 → 33，但累计耗时 172.595 → 172.536 秒基本不变，输入/输出 token 合计 236,800 → 237,388 基本不变，实际费用仍较高。因此，当前只能证明相对退化版复查减少了开销，不能声称学习后已经普遍比首次探索更快、更省。这是小样本历史对照，未控制随机生成与供应商缓存等因素。

仍有一轮对 `overlay-blocking` 误用绑定工具，被正确拒绝后进行了额外自由测量；最终学习规则只消费绑定测量，故没有重复规则发现。工具路由和重复观察仍可改进，此类已恢复的错误保留在记录中，不描述为全程零错误。

**M5 结论：** 确认 → 真实生成 → 正反/unknown 验证 → 人工审阅 → 隔离启用 → 六轮复查链路通过当前靶场的门槛。规则可复用，当前事实适配仍面向购物业务；上传、同步等真实场景、多目标语义判断、严格从响应时刻起算的 SLA 以及业务重试处理成功均未由本批证明。M4 的完整 18 轮没有在本次执行层修复版本上重跑，其历史冻结基线结论与本次 M5 结论分别保留。
