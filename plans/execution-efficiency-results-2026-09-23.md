# 执行效率开发与验证记录

日期：2026-09-23。状态：P0–P3 已实现，P4 验收进行中；不能据此宣布最新版本通过正式 M4/M5 或达到性能目标。

对应[开发计划](./execution-efficiency-plan.md)。首个 P4 候选为 `a638983`，运行期代码与 P3 提交 `854cd1d` 相同。该候选预检 5/6，C5 未通过，已停止扩大样本并修复；新候选待重新冻结。

## 本轮实现

| 阶段 / 提交 | 实际交付与边界 |
| --- | --- |
| P0 / `9a7853e` | 有父子关系的耗时追踪，区分互斥 wall time 与嵌套累计时间；历史 M5 六轮统计重现；独立 Git 归档、构建、数据库和端口的 A/B runner。仅仪表化，没有改 Agent 提示词和决策。 |
| P1 / `d7c8bc0` | 动作返回已保存检查、当前观察和收尾建议；动作前可验证的观察复用。节点身份、样式、命中、滚动和业务响应变化会失效；动画、媒体、表单、shadow DOM 等无法证明稳定的页面继续完整采集。 |
| P2 / `2810e2e` | 规则路由元数据、最多五条默认候选摘要、分页搜索与详情工具；相同事实下复用自动检查。缺元数据/触发资格仍为 unknown，未绑定学习规则不能成为 pass，待检查项进入收尾核验。 |
| P3 / `854cd1d` | 从已结束 Run 的证据提取 2–3 步导航片段；当前页面逐步验证目标、前后条件和质量异常。网络写屏障持续至下一次明确单步操作。没有零写入记录的历史轨迹不能变成可复用路径；不自动重放下单和支付。 |
| 验收入口 / `a638983` | 两例学习诊断与六例未知发现分库；支出预留上限、主/视觉模型分别核验供应商、实际版本和配置记录。学习诊断不混入正式 M5 分母。 |

本地验证：冻结候选 38 个测试文件、282 项测试通过，格式、类型与构建通过。P3 构建的六个生产靶场及私有隔离检查通过。浏览器测试覆盖观察变化失效、复用期间插入遮罩、行为变成写操作时拦截、歧义和取消；不能用这些确定性测试替代真实 Agent 质量验收。

## 分阶段真实诊断

每阶段固定 C0/C2，各组各一次，共四轮。顺序为基线 C0、候选 C0、候选 C2、基线 C2；每轮 300 秒、30 次请求、40 次动作。主模型 DeepSeek V4.1 Flash / Wafer，视觉模型 Qwen3.7 Plus；没有增加预算、删去困难场景或重试整批。每批估算支出上限 $0.50。

表内请求、时间、token 是每组两轮之和；token 包含输入与输出。全部请求均保留，usage 与费用完整。阶段间基线不同，不能把四批合成一个 A/B 样本。

| 对照阶段 | 质量 | 请求：基线 → 候选 | 时间：基线 → 候选 | token：基线 → 候选 | 四轮费用 |
| --- | --- | --- | --- | --- | --- |
| P0 | 4/4 | 29 → 25 | 274.6 → 201.5s | 213,946 → 185,343 | $0.057120 |
| P1 | 4/4 | 21 → 15 | 119.0 → 193.1s | 143,241 → 124,626 | $0.039524 |
| P2 | 4/4 | 13 → 16 | 84.5 → 192.9s | 99,615 → 138,923 | $0.037866 |
| P3 | 4/4 | 18 → 14 | 240.3 → 154.5s | 159,415 → 118,871 | $0.045945 |

P0 只有测量变化，已有明显自然波动，不能把它的结果称作提速。P1 虽然少了约 29% 请求，总时间增加约 62%；P2 两轮总请求、时间、token 都增加。P3 两轮总值改善，但 C0 从 18.0s 增至 25.3s，C2 从 222.3s 降至 129.2s。它们都是单次诊断，性能结论必须以后面的固定配对实验为准。

P2 候选 C2 的 176.7s 中约 160.7s 为模型请求等待；工具记录显示重复观察、元素详情与规则检索，以及恢复过的点击错误。页面采集减少并不保证模型少犹豫。P3 候选 C2 实际调用一次导航片段，第二步后遇到新遮罩即交回，最后有证据地报告阻断；没有把导航完成当作检查完成。

原始记录（本机忽略目录，不随 Git 分发）：

- P0: `data/efficiency/2026-09-23T09-42-29-466Z/`。
- P1: `data/efficiency/2026-09-23T09-51-03-181Z/`。
- P2: `data/efficiency/2026-09-23T09-58-00-638Z/`。
- P3: `data/efficiency/2026-09-23T10-11-53-865Z/`。
- 历史统计复现：`data/efficiency/historical-baseline.json`，59 次请求、275,687ms 模型等待、80 次完整观察、372 次规则事件。

## P4 冻结验收

首批 `a638983`：`data/acceptance/2026-09-23T10-18-50-245Z/`。真实 DeepSeek/Qwen smoke 通过，C0–C4 通过，C5 needs-review，5/6；没有进入学习两例、十二轮对照或正式十八轮。C5 用 7 次请求、41.4s，缺陷方向正确但证据无效：Agent 手抄 CSS 路径漏掉一层 div，五秒测量 21 个样本均为 null，仍将缺陷提交为 supported。私有评分器正确拒绝，原运行与错误发现完整保留。

修复采用当前 elementRef 固定目标节点，避免抄写路径；测量显式区分 unknown 与 false，引用 unknown 测量时拒绝 supported/refuted。没有修改 C5 评分门槛、批准规则声明或五秒窗口。针对真实故障新增正常/禁用目标、过期引用、unknown 证据不得成为确定结论的浏览器回归。

后续验证进行中。真实 smoke 与八例预检完成后，才运行十二轮学习复查 A/B。性能门槛仍为请求下降至少 20%、总时间下降至少 15%、输入加输出 token 不增，且质量通过。

当前没有启动新正式 M4 的十八轮；历史 `ae95c91` 的 18/18 与 `af85312` 的 M5 6/6 仍只属于对应冻结版本。最终结论与所有失败记录会在这里追加，不覆盖之前批次。
