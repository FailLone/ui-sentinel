# 执行效率开发与验证记录

日期：2026-09-23。状态：P0–P3 已实现，P4 八例预检与十二轮对照完成；本批质量通过，性能目标未达且 token/费用比较不完整。按预设门槛，没有启动新的正式 M4 十八轮。

对应[开发计划](./execution-efficiency-plan.md)。首个 P4 候选为 `a638983`，运行期代码与 P3 提交 `854cd1d` 相同。该候选预检 5/6，C5 未通过，已停止扩大样本并修复；第二候选 `fe0c6eb` 通过 smoke 与八例预检；随后对照因基线采样调度缺陷停止。共同修复提交为 `0588a03`（候选）/ `bf0dd39`（基线）；最终候选冻结 `9d8d4e5`，运行期与 `0588a03` 相同。

## 本轮实现

| 阶段 / 提交 | 实际交付与边界 |
| --- | --- |
| P0 / `9a7853e` | 有父子关系的耗时追踪，区分互斥 wall time 与嵌套累计时间；历史 M5 六轮统计重现；独立 Git 归档、构建、数据库和端口的 A/B runner。仅仪表化，没有改 Agent 提示词和决策。 |
| P1 / `d7c8bc0` | 动作返回已保存检查、当前观察和收尾建议；动作前可验证的观察复用。节点身份、样式、命中、滚动和业务响应变化会失效；动画、媒体、表单、shadow DOM 等无法证明稳定的页面继续完整采集。 |
| P2 / `2810e2e` | 规则路由元数据、最多五条默认候选摘要、分页搜索与详情工具；相同事实下复用自动检查。缺元数据/触发资格仍为 unknown，未绑定学习规则不能成为 pass，待检查项进入收尾核验。 |
| P3 / `854cd1d` | 从已结束 Run 的证据提取 2–3 步导航片段；当前页面逐步验证目标、前后条件和质量异常。网络写屏障持续至下一次明确单步操作。没有零写入记录的历史轨迹不能变成可复用路径；不自动重放下单和支付。 |
| 验收入口 / `a638983` | 两例学习诊断与六例未知发现分库；支出预留上限、主/视觉模型分别核验供应商、实际版本和配置记录。学习诊断不混入正式 M5 分母。 |

本地验证：首候选 38 个测试文件、282 项测试通过；C5 证据修复后的 `fe0c6eb` 为 285 项测试通过，格式、类型与构建通过。P3 构建的六个生产靶场及私有隔离检查通过。浏览器测试覆盖观察变化失效、复用期间插入遮罩、行为变成写操作时拦截、歧义和取消；不能用这些确定性测试替代真实 Agent 质量验收。

## 分阶段真实诊断

每阶段固定 C0/C2，各组各一次，共四轮。顺序为基线 C0、候选 C0、候选 C2、基线 C2；每轮 300 秒、30 次请求、40 次动作。主模型 DeepSeek V4.1 Flash / Wafer，视觉模型 Qwen3.7 Plus；没有增加预算、删去困难场景或重试整批。每批估算支出上限 $0.50。

表内请求、时间、token 是每组两轮之和；token 包含输入与输出。全部请求均保留，usage 与费用完整。阶段间基线不同，不能把四批合成一个 A/B 样本。

| 对照阶段 | 质量 | 请求：基线 → 候选 | 时间：基线 → 候选 | token：基线 → 候选 | 四轮费用 |
| --- | --- | --- | --- | --- | --- |
| P0 | 4/4 | 29 → 25 | 274.6 → 201.5s | 213,946 → 185,343 | $0.057120 |
| P1 | 4/4 | 21 → 15 | 119.0 → 193.1s | 143,241 → 124,626 | $0.039524 |
| P2 | 4/4 | 13 → 16 | 84.5 → 192.9s | 99,615 → 138,923 | $0.037866 |
| P3 | 4/4 | 18 → 14 | 240.3 → 154.5s | 159,415 → 118,871 | $0.045945 |

P0 只有测量变化，已有明显自然波动，不能把它的结果称作提速。P1 虽然少了约 29% 请求，总时间增加约 62%；P2 两轮总请求、时间、token 都增加。P3 两轮总值改善，但 C0 从 18.0s 增至 25.3s，C2 从 222.3s 降至 129.2s。它们都是单次诊断，性能结论必须以后面的固定配对实验为准。

P2 候选 C2 的 176.7s 中约 160.7s 为模型请求等待；工具记录显示重复观察、元素详情与规则检索，以及恢复过的点击错误。页面采集减少并不保证模型少犹豫。P3 候选 C2 实际调用一次导航片段，第二步后遇到新遮罩即交回，最后有证据地报告阻断；没有把导航完成当作检查完成。

原始记录（本机忽略目录，不随 Git 分发）：

- P0: `data/efficiency/2026-09-23T09-42-29-466Z/`。
- P1: `data/efficiency/2026-09-23T09-51-03-181Z/`。
- P2: `data/efficiency/2026-09-23T09-58-00-638Z/`。
- P3: `data/efficiency/2026-09-23T10-11-53-865Z/`。
- 历史统计复现：`data/efficiency/historical-baseline.json`，59 次请求、275,687ms 模型等待、80 次完整观察、372 次规则事件。

## P4 冻结验收

首批 `a638983`：`data/acceptance/2026-09-23T10-18-50-245Z/`。真实 DeepSeek/Qwen smoke 通过，C0–C4 通过，C5 needs-review，5/6；没有进入学习两例、十二轮对照或正式十八轮。C5 用 7 次请求、41.4s，缺陷方向正确但证据无效：Agent 手抄 CSS 路径漏掉一层 div，五秒测量 21 个样本均为 null，仍将缺陷提交为 supported。私有评分器正确拒绝，原运行与错误发现完整保留。

修复采用当前 elementRef 固定目标节点，避免抄写路径；测量显式区分 unknown 与 false，引用 unknown 测量时拒绝 supported/refuted。没有修改 C5 评分门槛、批准规则声明或五秒窗口。针对真实故障新增正常/禁用目标、过期引用、unknown 证据不得成为确定结论的浏览器回归。

修复后预检：`fe0c6eb`，真实 smoke 与 C0–C5 全部通过，数据为 `data/acceptance/2026-09-23T10-28-55-945Z/`。C5 使用当前 elementRef，七秒窗口内 28 个样本均为 false；这次得到的是完整测量，非上一批的 null。正常、异常学习两例来自另一隔离数据库 `data/efficiency/2026-09-23T10-33-18-913Z/`，2/2，通过同一已批准规则的五秒完整绑定门槛；未修改源学习数据库。两例合计 12 请求、115.0s，不能当作正式六轮 M5。

八例通过后开始第一批十二轮配对 A/B。基线 `9a7853e` 是原 `e4c9ecf` 加 P0 仪表化，提示词、工具和决策保持原样；候选 `fe0c6eb`。DeepSeek 固定 Wafer，Qwen 固定 Alibaba；两组使用相同批准声明与私有评分器。性能门槛仍为请求下降至少 20%、总时间下降至少 15%、输入加输出 token 不增，且质量通过。

因最终性能门槛未通过，没有启动新正式 M4 的十八轮；历史 `ae95c91` 的 18/18 与 `af85312` 的 M5 6/6 仍只属于对应冻结版本。最终结果如下；所有旧批次与失败保持原样。


### 首批对照中止与共同修复

`data/efficiency/2026-09-23T10-35-55-800Z/`：计划十二轮，实际仅基线异常首轮，16 次请求、300,014ms 后超时，质量不通过；候选未运行。本批不产生速度结论，也不混入新批分母。

绑定正确、样本均 false，但原采样方式是在每次浏览器读取后再等待 250ms，约 13ms 的读取时间逐次累积。真实样本相对开始时刻为 `3,266,530,790,1055,1325,1586,1846,2109,2372,2640,2895,3157,3420,3683,3944,4208,4471,4738,5002`；5002ms 超出规则五秒窗口，最后一个有效样本为 4738ms，边界缺口 262ms 超过允许的 250ms。因此 unknown 正确，重复观察不能修复此前不完整的测量。

修复将采样锚定到每 200ms 的时钟网格，覆盖完整五秒并为一般读取抖动留余量；较大实际缺口、缺失目标仍为 unknown。原声明、评分器、端点容差和预算完全不改。确定性测试重放原失败时间戳，仍得到 unknown；13ms 读取延迟的新轨迹在 5013ms 结束并正确判 fail，正常目标 pass，600ms 慢读取与 null 样本仍 unknown，取消后不继续采样。

为了公平比较，`0588a03` 的同一修复原样 cherry-pick 到 P0，形成 `bf0dd39`；后者仅比 `9a7853e` 多此采样修复，不包含 P1–P3 或探索工具变更。本机分支 `codex/efficiency-baseline-sampling` 保存该冻结基线。两组采集器与调用片段哈希相同。候选全套 288 项测试、类型和构建通过；新一批重新固定 SHA，保留旧失败，不把基线缺陷造成的超时当作优化收益。


## 最终冻结批次与结论

- 候选 `9d8d4e55a068929da58340b933a1d69537b0d6b0`；基线 `bf0dd39`。相同采样器、相同批准声明、相同模型/固定供应商、每轮 300s/30 请求/40 动作，单请求 60s、允许一次安全重试、工具 15s。没有按结果修改阈值或重跑同一批次。
- 最终 smoke 与 C0–C5 预检：`data/acceptance/2026-09-23T10-45-09-922Z/`，6/6。C5 无学习规则，通过 elementRef 自主测量并发现；本次 Agent 选择 12s 窗口，61 个样本均 false，不据此宣称测量已最省。
- 最终学习预检：`data/efficiency/2026-09-23T10-49-34-282Z/`，异常/正常 2/2；13 请求，146.5s。与未知发现数据库隔离。
- 正式配对对照：`data/efficiency/2026-09-23T10-52-28-448Z/`，两组共 12/12 质量通过，源数据库哈希未变。候选六轮均满足 M5 的 bound-rule-1 质量契约：每轮一次完整五秒绑定检查、异常一个发现、正常零误报、证据有效、单订单、主动结束。这是同一组 A/B 样本，不是另一批独立 M5，也不等于完整新版本放行。

| 样本 | 请求：基线 → 候选 | wall time 秒：基线 → 候选 | 总 token：基线 → 候选 | 质量 |
| --- | --- | --- | --- | --- |
| 异常 1 | 8 → 7 | 73.522 → 63.534 | 55,788 → 58,266 | 两组通过 |
| 异常 2 | 8 → 5 | 43.940 → 51.438 | 55,749 → 41,808 | 两组通过 |
| 异常 3 | 11 → 6 | 66.993 → 44.978 | 82,127 → 51,000 | 两组通过 |
| 正常 1 | 9 → 6 | 74.508 → 117.266 | 62,741 → unknown | 两组通过 |
| 正常 2 | 8 → 5 | 50.986 → 24.833 | 54,135 → 39,204 | 两组通过 |
| 正常 3 | 7 → 5 | 36.486 → 48.670 | 46,501 → 40,187 | 两组通过 |

| 汇总 | 基线 | 候选 | 变化 |
| --- | --- | --- | --- |
| 六轮请求总数 | 51 | 34 | −33.3%，达到请求目标 |
| 六轮总耗时 | 346.435s | 350.719s | +1.2%，未达到 −15% 目标 |
| 六轮中位耗时 | 58.990s | 50.054s | −15.1%，仅描述分布，不能代替总时间门槛 |
| 六轮最大耗时 | 74.508s | 117.266s | 候选较差，不推断 p95 |
| 异常组总耗时 | 184.455s | 159.950s | −13.3% |
| 正常组总耗时 | 161.980s | 190.769s | +17.8% |
| 异常组中位 / 最大耗时 | 66.993s / 73.522s | 51.438s / 63.534s | 三次样本 |
| 正常组中位 / 最大耗时 | 50.986s / 74.508s | 48.670s / 117.266s | 三次样本 |
| 模型请求等待总量 | 308.316s | 310.244s | 两组均约占运行时间 89% |
| 总 token | 357,041 | unknown | 一次失败请求缺 usage，无法验证不增加目标 |

正常候选第 1 轮有一次上游请求超时，客户端按原 60s 请求期限取消并安全重试，最终质量通过；实验代理未立即转发客户端断连，上游请求在原代理 90s 上限结束，usage/供应商回执缺失。这次失败计入请求数与时间，绝不当作零 token/零费用或从分母剔除。整批可报告已观测的请求与 wall time，完整 token/成本/实际路由比较为不可比。正常组退化不能被异常组改善掩盖。

本批已知回执费用合计 **$0.075351**，另有 **1** 条请求费用未知。旧网关 `accountedUsd=$0.082846` 包含约 **$0.007495** 未知请求预留，不能称作实际账单。模型当日单价保存在该目录 `models.json`；各请求的缓存命中、usage、实际供应商、费用原始值保存在 ledger/请求记录中。缺 usage 的样本，其缓存/token/成本合计同样不完整。

### 实验工具的后续修正

本批结束前定位并单独修正了两个实验工具问题，运行中的冻结进程不热更新：

1. 汇总遇到未知 token/elapsed 保留 null；原版本将未知样本加为零，虽然 comparable=false 阻止错误放行，数值仍容易误导。原 `summary.json` 不覆盖，另存 `audited-summary.json`，记录原文件哈希、null 修正与费用拆分，质量评分不变。
2. 实验网关在下游断连时立即取消上游请求，并等待请求账本落盘；未知费用继续保留估算预留，单列 knownCostUsd 和 unknownReservedUsd。取消不保证供应商不计费。真实故障触发的修复用可控上游契约测试验证，不为取得更好的数字重跑这组付费样本。

这些修改限于私有实验编排、汇总和测试，应用执行代码保持冻结候选。没有增加预算或修改已批准规则。

### 放行与后续

**本批质量通过，性能目标未达；完整版本尚未放行。** 新正式 M4 十八轮按计划不启动，历史十八轮仍只属于旧冻结版本。P0–P3 的实际交付可以评审和继续开发，但不能宣传为已经稳定提速或最优架构。

请求数下降验证了减少重复决策和复用路径的价值；主耗时仍在模型请求等待。下一轮应单独对照模型请求时延、收尾输出长度与必要的规则检索开销，保留质量门槛与超时失败。若试供应商/模型或流式协议，另立固定协议，不能混进本次代码对照结论。G1 业务适配、G2 主动视觉发现仍未实施。


最终交付校验：39 个测试文件、290 项测试、类型检查、构建及格式检查通过。实验工具后续修正没有改变服务端运行构建；最终 `dist/server/index.js` 的 SHA-256 与冻结候选完全相同（`ce7a1f0e13bc459044785d1583d6555757c60761c75e89163ec443a5e21525a4`）。

缓存与单价口径：基线缓存命中 232,960 个输入 token；候选有回执请求命中 133,888 个，另 1 条请求未知，不能作为完整缓存率比较。`models.json` 的时段覆盖在本批 UTC 时段给出的 DeepSeek 参考价为输入 $0.30/百万、输出 $1.20/百万、缓存读取 $0.006/百万；Qwen 的普通上下文参考价为输入 $0.32/百万、输出 $1.28/百万。列表包含时段/上下文梯度覆盖，实际账单仍以请求回执为准，未知不能用参考单价伪造。
