# 最小可验证路径 — 当前进度

更新：2026-09-22。本记录区分已实现、确定性验证与真实模型验收；交付范围和门槛以 [最小验证计划](./minimum-validation-plan.md) 为准。

## 已实现与验证

- Mastra Agent、Playwright 交互/取证、Midscene 视觉定位；规则、假设、测量、发现和业务终态接通。
- 串行队列、取消、预算、未知写结果阻断、重启核对；私有靶场控制与答案隔离。
- 截图与红框副本、命中采样、最终可见反馈计时；工作台、持久事件与报告。
- 声明式学习候选、正反例与 unknown 验证、人工批准、启用后加载。
- 执行记忆修复：保留动作参数/结果/证据，旧记录按需读取；元素引用、歧义点击、新鲜度与失败请求统计已修复。
- Node 24.21.0、pnpm 10.17.1；构建、类型检查、172 项确定性测试及 C0–C5 靶场检查已通过。靶场检查不是 Agent 能力评估。
- 真实 DeepSeek 工具调用和 Qwen 视觉定位 smoke 已通过。修复后 C0 操作复验 2/3 完整通过；另一轮已支付但结束前上游连接中断，仍记失败。详见 [实验结果](./browser-loop-experiment-results.md)。

## 尚未完成的验收

| 阶段 | 当前边界与剩余工作 |
| --- | --- |
| M0 | 构建、浏览器、SDK 契约及真实模型/视觉 smoke 已验证 |
| M1 | 六个靶场与私有重置/验证已完成 |
| M2 | C0 购买操作已有真实验证；其他路径、自主探索与稳定性仍需评估 |
| M3 | 规则和证据已接通；真实 Agent 在 C1/C2 的稳定检出未验收 |
| M4 | 18 轮评估命令已实现；C0–C5 × 3 真实模型批次尚未验收 |
| M5 | 学习闭环已实现并确定性测试；真实生成及启用前后效果/成本比较待验证 |

下一步按 README 执行固定批次评估及反馈复查。保留失败记录，不以操作任务通过率代替缺陷发现能力，也不以节省 token 抵消漏报。

## 后续优化边界

旧效率优化和循环退化排查计划已被实际修复及实验报告取代，删除重复计划；历史版本可在 Git 中查阅。尚未实施的多步循环、观察复用、无进展策略和视觉输入区域采样不视为已完成。若继续推进，先固定质量基线再独立对照，不能强制点击、提前结束或缩减采样来隐藏问题。

当前仍为受信单机、购买靶场专用实现；分布式执行、跨业务通用性和未知体验问题发现需要独立验收。学习规则继续由人工批准。
