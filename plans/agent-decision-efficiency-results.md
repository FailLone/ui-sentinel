# Agent 决策效率实验结果

状态：进行中。P0 离线审计完成，P1 固定输入实验完成但候选未通过；P2 正在进行源码兼容性核查和适配，完整任务比较尚未执行。

对应 [计划](./agent-decision-efficiency-plan.md)。应用基线 `28a7111`，原始记录来自 `data/acceptance/2026-09-23T15-47-07-984Z/`。本轮首先不改默认执行器，不改变模型或检查范围。

## P0：审计方法与证据

运行 `pnpm exec tsx scripts/experiments/decision-audit.ts`，生成 `data/decision-audit/default-2026-09-23/audit.json`。该离线脚本关联实际发送的 requests、实际返回的 responses、网关 ledger、报告事件及下一请求收到的工具结果，不调用模型。记录原文件和每个输入的 SHA-256，保留输入页面版本、历史窗口、工具参数、结果与已提供证据。

完整覆盖 C2 十三次、C5 十次请求。下表耗时为网关 `durationMs`，与服务端请求生命周期计时略有差别；不是工具执行时间。表中的“目的”是根据工具与参数作出的审计解释，不是模型内部推理记录。

### C2：有充分拦截证据后，调查仍绕行

| 请求 | 秒 | 快照 | 实际动作与新增信息 | 判断与依赖 |
| --- | ---: | --- | --- | --- |
| 1 | 1.052 | s1 | 加入购物车 | 必要业务前置，产生新状态 |
| 2 | 1.238 | s3 | journey 进入结算；自动规则报告四个动作被拦截 | 必须依赖购物车；产生阻断证据并交回 Agent |
| 3 | 5.756 | s6 | 读取 Pay、Back、活动标题与段落详情，获得共同拦截者 selector | 有价值的首次定位；元素 blockedPoints 和规则发现此前已提供 |
| 4 | 18.765 | s6 | 空查询规则库；对 Pay 做 trial probe，确认 pointer interception | probe 增加独立动作证据；空查询返回与输入 knownRules 相同的两项，检索无新增规则 |
| 5 | 13.310 | s7 | 再读同组元素的新 ref，拦截者相同 | 新快照下重新定位，不能仅按工具名算重复；业务现象未变化，需解释 ref 更新是否值得一轮决策 |
| 6 | 4.601 | s7 | 读取 probe 结果 3.1 的首段 JSON | 原错误已完整出现在本次历史；首段未提供新的交互证据 |
| 7 | 8.702 | s7 | 继续读 3.1，从 offset 916 起 | 上一步分页诱发依赖；仍是同一错误重复日志，未完成读取整个 4181 字符结果 |
| 8 | 3.306 | s7 | page_observe，得到 s8，页面内容仍相同 | 新时间点快照；没有此前未见的恢复入口，不是新独立测量 |
| 9 | 19.015 | s8 | 两条 rule_details 与 campaign 搜索 | 规则路由信息返回，但自动检查已保存；campaign 无匹配。三次工具共享一次模型请求，不能算三轮 |
| 10 | 14.472 | s8 | 读取活动区域与 Pay 的详情 | 能提供当前 ref/selector；大部分语义事实已见，但前次结果逐渐退出历史窗口 |
| 11 | 16.174 | s8 | 再次空查询规则库 | 相同适用规则已在当前输入 knownRules 中；没有新增规则事实 |
| 12 | 7.179 | s8 | 点击拦截者容器，尝试关闭 | 不应删除的恢复探索；工具返回没有消除阻断，s9 保存证据 |
| 13 | 5.279 | s9 | observed-blocker 收尾 | 主动结束；不能把它替换为执行器提前结束 |

可以合并呈现的是已有命中信息、已检查规则与 probe 的失败事实，不能提前虚构第 12 步的关闭结果。第 6/7 次合计 13.303 秒用于读已经提供的错误；第 11 次 16.174 秒再次查已有目录。这些是可解释的调查开销，但不能把其总和直接当作可保证节省的时间。

### C5：未知问题发现正确，正式取证启动较晚

| 请求 | 秒 | 快照 | 实际动作与新增信息 | 判断与依赖 |
| --- | ---: | --- | --- | --- |
| 1 | 6.278 | s1 | 加入购物车 | 必要业务前置 |
| 2 | 2.959 | s3 | journey 进入结算 | 有新状态，复用已验证导航 |
| 3 | 4.438 | s6 | 支付；公开响应 failed、canRetry=true，重试按钮 disabled | 关键触发；之后禁止第二次业务写入 |
| 4 | 18.746 | s7 | 查 retry 规则、读按钮和返回入口详情、空查询目录 | retry 无匹配；详情是首次调查。目录摘要此前已完整提供；三个工具共享一次模型请求 |
| 5 | 16.433 | s7 | page_observe，仍为 disabled | 新时间点快照，不能证明整个五秒窗口持续不可操作 |
| 6 | 14.980 | s8 | 再次 page_observe，仍为 disabled | 同上；两次观察共 31.413 秒，随后仍必须正式测量 |
| 7 | 9.881 | s9 | 提出未知重试缺陷假设 | 必要语义判断；不能用已有规则检查通过替代它 |
| 8 | 7.750 | s9 | transition_observe，选择六秒动作可用性窗口 | 依赖已登记假设，产生 31 个样本、最大间隔 207ms、均不可操作。工具测量时间另计 |
| 9 | 9.570 | s10 | 用完整窗口证据提交 supported 发现 | 必须依赖测量结果；仍需限制结论到被测窗口，不把 never 当作无限时间证明 |
| 10 | 6.946 | s10 | scope-covered 收尾 | 假设已处置，主动结束 |

不能并行支付与重试检查，也不能并行第 7–9 步的因果依赖。可以更早把公开响应、禁用按钮、规则目录和缺失测量放到一起，让 Agent 更快启动有价值的未知问题调查；这是待验证假设。

## 确认与未知

- 确认：两例的元素与规则摘要通常已在每次输入中，继续增加规则检索工具并不能自动解决重复调用。C2 的原始错误分页和 C5 的离散观察是具体开销来源。
- 确认：C2 的 ref 从 s6 到 s7/s8 更新，历史窗口也发生裁剪；部分重新读取有定位/记忆原因，不能全部删除。当前持久规则摘要能避免规则事实完全消失，但没有阻止重新检索。
- 确认：模型多工具调用已存在，并非每个工具一定消耗独立模型往返。
- 未知：供应商排队、推理和生成分别占多久；无法由 reasoning token 比例解答。也不能断言改清楚输入就必然更快。
- 未知：Agent 是否会接受结构化汇总、从而减少重复确认，还是新增摘要反而增加输入和推理负担。需要同输入重放。

## P1：冻结候选与评分约定

首个候选 `investigation-digest-1` 只改变信息组织：在原始输入前添加最多 6000 UTF-8 字节的事实索引，将当前可操作元素、规则与已有检查、业务响应、未决假设和历史工具回执放在一起。原始输入、工具 schema、原始读取入口、模型及预算全部保留；不读未来结果、不查询私有答案、不提前判定完成。

历史回执明确标为历史，元素详情只允许当前 ref 且无过期标记/快照冲突；保留原始证据引用。重复错误行归并只抽取原错误已包含的 timeout、disabled、not visible、pointer interception 行，不生成像素遮挡结论。超限删除整条可选索引项并标明遗漏，原始事实仍在。

这轮刻意不同时修复分页、稳定 ref、改提示词或更换框架，以免混淆归因。索引生成无新工具/模型调用，但会增加输入字节，必须计入费用；只有实验通过才能考虑集成。

固定状态：规则检索 C2/4、C5/4；重复核验 C2/6、C5/5；收尾 C2/13、C5/10。选择的是每类第一次出现的代表性问题和两例最终决策，没有按候选输出事后选择样本。

每份输入两种契约、各两次，交替执行，共 24 请求，无浏览器/工具副作用，零重试，整批估算上限 $0.75。脚本 `scripts/experiments/decision-replay.ts` 在请求前持久化提交、模型列表价格、输入哈希、顺序、每状态语义评分约定；所有错误保存。合法工具调用仅是传输结果，必须复核参数与语义才能记作有效决策。

允许多种合理行为：已证实阻断后的具体恢复调查或诚实结束；未知重试问题的假设登记或补充缺失证据；最终状态的主动结束。重复检索已经完整提供的目录、单独读已有错误、用离散观察替代连续测量不记作调查进展。新探索必须指出实际未覆盖的问题；不存在通用的“只能调用指定工具”判定。

准备验证：三个输入契约回归测试及 TypeScript 检查通过。

### P1 实际结果：未采用，不进入候选端到端比较

冻结提交 `438b933`，批次 `data/decision-replay/2026-09-23T16-23-10-580Z/`，24 个请求全部执行。`manifest.json` 保存运行前评分约定，`records.json` 保留原始结果，`semantic-review.json` 保存逐条人工证据审核及理由。没有执行任何返回工具。

| 指标 | 原始输入 | 增加事实索引 |
| --- | ---: | ---: |
| 有效下一步 | 7/12 | 7/12 |
| 两例收尾状态 | 4/4 | 4/4 |
| C2 首次独立核验状态 | 0/2 | 2/2 |
| C2 已核验状态 | 0/2 | 0/2 |
| C5 初始未知发现状态 | 2/2 | 0/2 |
| C5 重复观察后状态 | 1/2 | 1/2 |
| 请求 wall time 合计 | 162.860s | 95.476s |
| 已知 input tokens | 89,134 | 111,810 |
| 已知 output tokens | 9,790 | 11,902 |
| usage 未知请求 | 1 | 0 |
| 已知费用 | $0.01519055 | $0.020064314 |

全批已知费用 $0.035254864，未知请求保守占额 $0.0049087，不能将占额称为实际费用。基线一次 60 秒请求超时保留在分母；其未知用量使费用和 token 总量不可完整比较，时间差也不能证明稳定提速。

“有效下一步”只表示从当前事实向合理调查/收尾前进，不表示完整验证已通过。五条提出假设的返回仍有测量计划表述不严谨：建议离散观察或 probe，并不能证明连续五秒未恢复；因仅登记待证假设且没有执行工具，本轮不将其当作 supported 发现。后续仍需真实完整测量。本轮没有错误完成或虚构 supported 证据，但事实索引把 C5 初始两次假设登记变成重复规则检索/观察，不能因另一个状态变好就忽略这个退化。

结论：首个事实索引假设没有稳定改善决策质量；C2 原始错误重读仍存在，C5 发现启动没有改善。不修改产品默认输入，不重复调提示词后跑同一批，也不进入 P2 的可选第四组。P2 继续当前执行器、Stagehand、Browser Use 三组完整任务比较。

## P2：适配前源码核查

- Stagehand 使用项目已锁定的 3.7.3；安装包许可证为 MIT。官方 [Agent 文档](https://docs.stagehand.dev/v3/basics/agent) 支持 DOM 模式、原生循环、自定义工具、取消与回调；本地模型类型支持 chat-completions 网关。保留完整循环，补接检查能力，不重复上一轮仅购买的比较。
- Browser Use 源码冻结 `d8110c5ff87ccba887aaa726cdb780f2f84bef8d`，版本 0.13.10，MIT，Python >=3.11。来源：[pyproject](https://github.com/browser-use/browser-use/blob/d8110c5ff87ccba887aaa726cdb780f2f84bef8d/pyproject.toml)、[OpenRouter 适配器](https://github.com/browser-use/browser-use/blob/d8110c5ff87ccba887aaa726cdb780f2f84bef8d/browser_use/llm/openrouter/chat.py)、[Agent](https://github.com/browser-use/browser-use/blob/d8110c5ff87ccba887aaa726cdb780f2f84bef8d/browser_use/agent/service.py)。独立 Python 3.12 虚拟环境已安装 PyPI 0.13.10，运行前检查实际安装版本和关键接口。源码阅读的提交与 PyPI 分发包分别记录，不宣称二者字节相同。
- Browser Use 原生 OpenRouter 适配器支持指定 base_url，但默认 max_retries=10；Agent 还默认启用 judge，可单独指定 extraction/judge 模型。这些必须显式配置并经统一网关计数，不能带着默认隐藏调用直接比较。DeepSeek 主循环使用 DOM，Qwen 单独作为视觉工具，不给文本模型静默混入截图。

源码核查只是适配依据，尚不是 Browser Use 在靶场上的成功运行记录。

### 原生循环兼容性预检：两组均通过

冻结提交 `5a8256b`。独立本地页面只包含一个标题，没有靶场业务和隐藏答案；给两组同一任务：读取当前标题，以结构化结果返回并结束。Stagehand 使用真实 `agent.execute()`，Browser Use 使用真实 `Agent.run()`，各自保留原生观察、历史和完成流程。DeepSeek/Wafer、low reasoning、4096 输出上限一致，最多三次请求、每请求 60 秒、单预检 90 秒、估算费用上限 $0.15。原生默认隐藏重试与 Browser Use judge 关闭；所有实际上游请求经过网关。

| 方案 | 原始数据目录（`data/oss-preflight/` 下） | 模型请求 | 已知费用 | 结果 |
| --- | --- | ---: | ---: | --- |
| Stagehand 3.7.3 | `2026-09-23T16-34-04-422Z-stagehand` | 3 | $0.000883503 | 正确标题、原生 completed、结构化 output |
| Browser Use 0.13.10 | `2026-09-23T16-34-39-494Z-browser-use` | 1 | $0.001041135 | 正确标题、原生 done/success、结构化 result |

两组用量完整，依赖清单和哈希存入各自 manifest。Stagehand 三次包含原生观察与最终结构化 done 请求；Browser Use 直接从原生初始页面观察作答。不能把这个小任务的次数差当作 UI 检查性能结论。两方案各使用一次预检机会，没有消费修正批次。

复现环境与命令：

```sh
uv venv --python 3.12 data/venvs/browser-use
uv pip install --python data/venvs/browser-use/bin/python browser-use==0.13.10
pnpm exec tsx scripts/experiments/oss-preflight.ts stagehand
pnpm exec tsx scripts/experiments/oss-preflight.ts browser-use
```

这些命令会进行付费预检；本轮已经执行，不因看到复现命令再重复调用。Python 环境只用于实验，应用依赖与默认执行器未替换。网关现在对各 SDK 统一限制单请求 60 秒，避免某个 SDK 的内部等待超过共同协议；网关与事实索引相关八项测试、类型和格式检查通过。

P2 尚未完成：接下来需要原生循环的共同质量工具适配，包括真实截图/DOM、自动规则、公开业务响应、假设登记、连续测量、证据提交及主动完成映射；正常交互写屏障和测量期间页面稳定性必须经过无模型验证。然后才冻结三方案 × C0/C2/C5 的九轮对照。不将读取标题的预检记作任一靶场验收。
